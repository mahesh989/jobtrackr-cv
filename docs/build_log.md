# Build Log (Archived from graph.json)

> This file preserves the detailed session narratives that were previously stored
> in graph.json build_state.completed entries. Moved during the graph.json refactor
> on 2026-05-23 to reduce token consumption from ~38K to ~10K.

## Session Narrative from _meta.updated

2026-05-22 (Bug-fix pass — three real bugs surfaced during user testing). (1) Migration 037: cover_letters.analysis_run_id was missing. Phase E-2's auto_cover_letter.py inserted that column on every auto-generation but it was never created (migration 025 predates the gated pipeline; analysis_run_id was added only to the new applications table in migration 031). Net: every E-2 attempt crashed with PG 42703, silently swallowed by the broad try/except. Diagnostic SELECT cl.analysis_run_id ... → 42703 revealed it. Added as nullable FK to analysis_runs with ON DELETE SET NULL + partial index for letters-by-run lookups. (2) Commit adc078b: jobs has no direct user_id column — ownership flows through jobs.profile_id → search_profiles.user_id (established back in migration 001). Two Phase F/G routes filtered jobs queries with .eq('user_id', user.id) which returned null with no error, surfacing as misleading 'job lookup failed' (cover-letter-pdf) and 'Job has no contact email' (send-email even when To: was clearly displayed). Fixed by dropping the bad filter; cover_letters.user_id check (one step earlier in each route) remains the authoritative ownership gate. (3) Migration 038: jd_quality auto-classification trigger. Migration 032 was a ONE-OFF backfill — jobs scraped after it stayed at jd_quality=NULL, so 'Needs JD' chip / TriageBanner / pipelineState='needs_jd' silently ignored every new job. AND the 032 threshold ('thin' < 300 chars) didn't match Phase E-1's skip threshold (worker skips < 2000), so 500-char teasers were 'unknown' per schema but auto-skipped by worker — user saw 5 Sydney jobs with no signal explaining why automation didn't run. Fix: classify_jd_quality() SQL function (single source of truth, 2000-char threshold matching worker JD_MIN_USABLE) + BEFORE INSERT/UPDATE trigger on jobs.{description,manual_jd_text} + full backfill that reclassifies every existing row (not just NULL). After 038, the 5 Sydney jobs reclassify NULL→'thin' and the existing UI surfaces them everywhere expected. — Earlier 2026-05-22 (Phase H-3 + D-3 + G-2 — Bulk send / inline letter editing / single PDF renderer). H-3 confirms the send model is user-initiated only — there is no auto-send code path. Adds bulk send capability via NEW EmailBulkBar component on the Ready-to-email tab: checkbox per card, Select-all toggle, sticky 'Send N emails' button, confirmation modal listing every recipient (irreversible action — gets real confirmation, not a one-click), sequential dispatch (await each send before next; Gmail quota ~250 units/sec, each send ~100), per-card status pills during/after batch (Sending/Failed), and a Batch-complete summary card when all rows clear. D-3 adds inline letter editing: NEW /api/applications/[letter_id] GET+PATCH route (GET returns full pass_3_final + timestamps for the modal to prefill; PATCH validates ownership, blocks edits to sent letters with 409, updates pass_3_final and clears pdf_storage_path so the next request lazy-re-renders); NEW EditLetterModal opens from a Pencil button on every non-applied card, lazy-loads the body, textarea with char-count + dirty indicator, Save → PATCH → router.refresh(). G-2 collapses the two PDF renderers (server-side outbox + client-side CoverLetterPanel) into one: extended /api/jobs/[id]/cover-letter/[letter_id]/download with ?format=pdf — same renderCoverLetterPdf helper; CoverLetterPanel.handleDownloadPDF now fetches bytes and triggers download via object-URL + synthetic anchor instead of building a jsPDF document client-side (~50 lines → ~15 lines, jsPDF import removed from CoverLetterPanel; TailoredCvCard still uses jspdf for its html2canvas path so the package stays in deps). Commits 6df6d69 (H-3) + c938c33 (D-3) + 9354a4c (G-2). — Earlier 2026-05-22 (Phase G + H-1 + H-2 — Cover letter PDF persistence, pool sidebar badge, bulk pool actions). Phase G: cover_letters now have a server-rendered PDF stored in a NEW Storage bucket 'cover-letters' (Migration 036 + RLS). Lazy generate-and-upload via ensureCoverLetterPdf — idempotent and called from both the new /api/applications/[letter_id]/cover-letter-pdf download route AND the send-email route (which now attaches BOTH the cover letter PDF and the tailored CV PDF — Phase F previously only attached the CV). jsPDF works in Node — renderCoverLetterPdf mirrors the existing client-side jsPDF layout exactly (A4, 0.8in margins, 11pt Helvetica, 1.35 leading). Email helpers (gmail.ts / outlook.ts) extended from single optional attachment to attachments[] array. ApplicationCard gains a 'Letter' download button on every tab. Phase H-1: layout.tsx counts cover_letters where status='completed' + is_stale=false + the joined job has all three of pool_decision_at/applied_at/dismissed_at NULL, passes as poolCount to SidebarNav which renders the existing NavItem badge slot on the Applications link. PostgREST !inner join with head:true+count:'exact' so the query returns just a number. Phase H-2: NEW PoolBulkBar wraps the pool tab — checkbox overlay per card, Select-all/Deselect-all toggle, sticky bottom action bar with 'No email' (bulk markPoolDecision no-email path) and 'Archive' (bulk dismiss). NEW server actions bulkMarkPoolNoEmail / bulkArchiveJobs in actions.ts both filter by .is(decision_col, null) so already-decided rows are no-ops. Intentionally NO 'Same email for all' — too risky if companies differ. Commits 863b698 (G) + 4dca9eb (H-1) + 2a68616 (H-2). — Earlier 2026-05-22 (Phase E-2 + Applications Pool revision + Phase F Gmail/Outlook OAuth dispatch. End-to-end automation now closes the loop: scrape → auto-analyze → auto-cover-letter (when passed_final_gate) → applications pool (user decides email channel) → in-app Send via Gmail/Outlook → marks applied. Five commits this session: (1) feat(automation): E-2 — orchestrator fires asyncio.create_task(auto_generate_cover_letter) after mark_run_completed when payload.automation && passed_final_gate; new cv-backend/app/services/automation/auto_cover_letter.py looks up job_id from run, idempotency-checks for existing non-stale letter, fetches voice profile + most recent story (no matching API — best available for automation), uses company_research_facts hook_text or generic fallback, inserts cover_letters row (status='generating'), fires run_cover_letter_pipeline. (2) D-1 revision — applications pool: new pool_decision_at timestamptz column on jobs (migration 033) gives explicit 3-state machine: NULL=pool, SET+contact_email=email tab, SET+no contact_email=apply tab. New 'To review' default tab in ApplicationStatusTabs; ApplicationCard renders amber decision box with email input in pool, Add email/No email buttons; new markPoolDecision server action. (3) Phase F — Gmail/Outlook OAuth email dispatch: migration 034 (oauth_token bytea→text + cover_letters.email_sent_at/email_sent_to), web/src/lib/email/{tokens,gmail,outlook}.ts (AES-256-GCM via existing encryptApiKey, Gmail RFC-2822 multipart/mixed → base64url, Outlook Graph fileAttachment, refresh logic for both providers), 5 new API routes (/api/auth/email/{google,outlook}/{,callback}, /disconnect, /api/applications/[letter_id]/send-email), EmailIntegrationCard with Connect Gmail / Connect Outlook / Disconnect, Send email button in ApplicationCard email tab. (4) Bug-fix pass: pool flow had three real bugs (jobs.has_email is GENERATED — code wrote to it → 42501; setTimeout-hide ran even on error → card vanished then reappeared silently; stale 'download CV manually' help text); ALL fixed. (5) Phase F debug: callback redirected with email_connected=success but email_integrations table stayed empty — root cause was schema check CHECK (provider IN ('gmail','outlook')) but code inserts 'google'/'microsoft'; migration 035 aligns constraint to code; saveTokens now throws on upsert error; callbacks try/catch and redirect with ?email_error=save_failed:<reason>; integrations page reads ?email_connected/?email_error and renders green/red banner. Required env: NEXT_PUBLIC_APP_URL, GOOGLE_CLIENT_ID/SECRET, optionally MICROSOFT_CLIENT_ID/SECRET. OAuth setup: Test users added in Google consent screen until verification, redirect URI https://jobtrackr-cv.vercel.app/api/auth/email/google/callback. Migrations 033/034/035 + cv-backend deploy + worker deploy + web deploy needed.) — Earlier: 2026-05-21 (Phase A — Migration 031+032 applied + verified. Schema foundation for the gated-pipeline overhaul: jobs gains jd_quality/role_match/has_email(GENERATED); analysis_runs gains 4 gate columns; search_profiles gains 6 automation-config columns with safe defaults (automation OFF by default); cover_letters gains auto_selected_variant_id; NEW applications table (the outbox — RLS + Realtime + 2 indexes); NEW email_integrations table (Phase F stub — schema only). Backfill classified 141 existing jobs: 37 rich / 41 thin / 63 unknown JDs; 74 match / 67 uncertain role-match. KEY FINDING: has_email = 1 — only ONE job in the pool has a contact_email, which means the apply_link channel will be the dominant flow; Phase F priority may shift toward JD email extraction before Gmail OAuth send. All 5 search_profiles received automation defaults. pipeline_state DROPPED from plan in favour of JS-derived state (same pattern as progressFlags.deriveProgress). No code reads new columns yet — Phase B is visibility-only UI.) — Earlier this session: 2026-05-20 (Phase 11 ship — voice-directed drafting: multi-variant opening paragraphs. cv-backend: opening_variants.py prompt (4 named patterns, single JSON-array call), GenerateOpeningVariantsRequest/Response schemas, /internal/generate-opening-variants endpoint (synchronous, HTTP 200), SYSTEM_BODY_ONLY + USER_TEMPLATE_BODY_ONLY prompts, _generate_body_with_chosen_opener() in generator.py, chosen_opening?: string added to GenerateCoverLetterRequest. Migration 027: DROP+ADD constraint adds 'picking' to status CHECK; opening_variants/chosen_opening/discarded_openings JSONB columns added. Web: cvBackend.ts (OpeningVariant, GenerateOpeningVariants* interfaces + function); /cover-letter POST phase-split: generateOpeningVariants() called before DB insert — cv-backend failure returns 502 without leaving stuck rows; row created with status='picking' + opening_variants payload; returns {letter_id, status:'picking', variants}; /cover-letter/[letter_id]/pick POST: auth → fetch letter (PickLetter cast) → 409 if not picking → find chosen → compute discarded → re-resolve CV/voice/key/story/JD → PATCH {chosen_opening, discarded_openings, status:'pending'} → generateCoverLetter(chosen_opening:) → 502 rolls back to 'picking'. Audit fix d61c048: company_name reads jobs.company not cover_letters.company_hook_text. CoverLetterPanel: extended CoverLetterRow + status union, Realtime SELECT updated, isPicking derived state, handlePick(), stacked-card VariantPicker UI with pattern_label + 'Use this opener' buttons. Commits: 8dbcf8c (11b), d61c048 (audit fix).) — Worker curl_cffi session (2026-05-20): scripts/fetch_jd.py + src/lib/curlfetch.ts + seekDirect curl_cffi upgrade (replaced got-scraping; location normalisation) + careerjet two-phase integration (API+tracking-URL-immediate-resolution+curl_cffi-JD-enrichment). SEEK: 22 jobs/run at $0 from Fly datacenter, no proxy. Careerjet: 20 jobs + 10/11 JDs at $0. Apify actor: fallback only, $0. Dockerfile: python3 + curl_cffi added. OPS-13+OPS-14 captured. Worker service status updated from copied_from_jobtrackr → active.

---

## next_action History

Session 2026-05-22 end state — code complete, partial user-verification, three things outstanding for the next session to pick up. SHIPPED THIS SESSION: Phase E-2 + applications pool revision + Phase F (Gmail/Outlook OAuth — verified working end-to-end, green badge shown) + Phase G (cover letter PDF persistence + email attachment) + G-2 (single PDF renderer) + H-1 (sidebar pool count badge) + H-2 (bulk No-email/Archive in pool) + H-3 (bulk send + confirmation modal in Ready-to-email) + D-3 (inline letter editing). Bug-fix pass: Migration 037 (cover_letters.analysis_run_id missing — Phase E-2 was silently crashing on every attempt), commit adc078b (jobs.user_id doesn't exist — two new routes filtered by it and threw misleading errors), Migration 038 (jd_quality trigger + threshold alignment — new jobs stayed at NULL after scrape, so 'Needs JD' chip never fired on them; threshold now matches worker's JD_MIN_USABLE=2000).

PENDING USER ACTION (next session must verify or guide user through):
  (a) DEPLOY commit adc078b: `cd web && vercel deploy --prod` — fixes 'job lookup failed' on Letter download and 'Job has no contact email' on Send button (Quantium card)
  (b) APPLY migrations 037 + 038 in Supabase SQL editor — 037 unblocks Phase E-2 from silently crashing, 038 makes thin-JD jobs visible in UI
  (c) USER TESTING incomplete: Letter download retry, Send email retry to rashmipoudel756@gmail.com, end-to-end bulk send via H-3 confirmation modal, EditLetter modal save round-trip. The Quantium letter is the test fixture in /dashboard/applications → To review (already moved to email tab after user pasted email).

NEXT PRIORITIES (after user verification passes):
  (E-3) Per-profile daily_application_limit enforcement — Redis counter at worker autoAnalyzeBatch level. Currently letters auto-generate with no cap; if a scrape returns 50 matches all pass the gate, all 50 letters get queued. Default cap 10/day. Simple counter at worker pre-trigger.
  (E-4) Dashboard banner — 'N new applications need review' complementing the sidebar badge so the signal is on the main dashboard too
  (Analytics) Per-source funnel: scraped → analyzed → passed-gate → letter → sent → applied. Right now we have no view of which sources actually convert.
  (Worker — separately) JD enrichment for thin-source teasers. Many SEEK/careerjet listings return 500-char teasers; worker could fetch the full listing page via curl_cffi (Phase 2 of careerjet already does this — extend pattern to other sources).

My recommendation: have the user verify (a)+(b)+(c) above first; once Letter + Send confirmed working, move to E-3 (single Redis counter, ~1h).

---

Earlier next_action: Phase A + B + C (1/2/3) + D-1 (revised) + D-2 + D-3 + E-1 + E-2 + F (verified) + G + G-2 + H-1 + H-2 + H-3 ALL DONE (2026-05-22). Full Applications experience is now: scrape → auto-analyze (E-1) → auto-cover-letter (E-2) → Applications Pool with sidebar count badge (H-1) → bulk pool decisions (H-2) → email channel decision → Ready-to-email tab → optional inline letter edit (D-3) before send → individual or bulk Send (H-3) via Gmail/Outlook with both PDFs attached (F + G) → marks applied. Single PDF renderer (G-2). Send model is strictly user-initiated — no auto-send code path. Required migrations: 033/034/035/036; env on web: NEXT_PUBLIC_APP_URL, GOOGLE_CLIENT_ID/SECRET, optionally MICROSOFT_CLIENT_ID/SECRET. NEXT priorities: (E-3) per-profile daily_application_limit enforcement (Redis counter at the worker level — currently letters auto-generate with no cap); (G-3) Phase E-2 cover letters miss the eager PDF render — auto_cover_letter.py could call cover-letter-pdf endpoint after pipeline completes to warm the cache, but lazy-on-first-download works fine too; (Analytics) per-source funnel scraped → analyzed → passed-gate → letter → sent → applied; (notification banner) E-4 — dashboard banner 'N applications need review' to complement the sidebar badge. My recommendation: E-3 next — single Redis counter, simple guard at the worker's autoAnalyzeBatch level, prevents runaway letter generation if you ever scrape a large new source. After that the system is functionally complete and the remaining items are pure polish/analytics.

Earlier next_action: Phase A + B + C (1/2/3) + D-1 (revised) + D-2 + E-1 + E-2 + F (verified) + G + H-1 + H-2 ALL DONE (2026-05-22). End-to-end automation loop is complete + polish landed: scrape → auto-analyze (E-1) → auto-cover-letter when passed_final_gate (E-2) → Applications Pool with sidebar count badge (H-1) → bulk No-email/Archive in pool (H-2) → email channel decision → Send via Gmail/Outlook with BOTH letter PDF + CV PDF attached (G + F) → marks applied. Required migrations: 033 + 034 + 035 + 036; required env on web: NEXT_PUBLIC_APP_URL, GOOGLE_CLIENT_ID/SECRET, optionally MICROSOFT_CLIENT_ID/SECRET. NEXT priorities: (E-3) per-profile daily_application_limit enforcement (Redis counter, default 10/day) — currently letters auto-generate with no cap; (D-3) inline letter editing in outbox cards before Send (textarea + save → updates pass_3_final → re-renders PDF on next request); (G-2) move CoverLetterPanel.handleDownloadPDF to use the same /api/applications/[letter_id]/cover-letter-pdf route so the client-side jsPDF dependency can be removed entirely; (G-3) Phase E-2 cover letters miss the auto PDF render — auto_cover_letter.py should call /api/.../cover-letter-pdf after pipeline completes to warm the cache (or accept lazy render on first download); (analytics) per-source funnel view scraped→analyzed→passed-gate→letter→sent→applied. My recommendation: E-3 next — single Redis counter, simple guard at the worker's autoAnalyzeBatch level, prevents runaway letter generation if you ever scrape a large new source.

Earlier next_action: Phase A + B + C (1/2/3) + D-1 (revised) + D-2 + E-1 + E-2 + F ALL DONE + VERIFIED (2026-05-22). End-to-end automation loop is complete and tested: Connect Gmail succeeds (green banner + green box with from_address), email_integrations row populated, encrypted oauth_token stored. Full chain: scrape → auto-analyze (E-1) → auto-cover-letter when passed_final_gate (E-2) → Applications Pool (To review tab) → user decides email channel → Send via Gmail/Outlook OAuth (F) → marks applied. Phase F bug-fix pass shipped: migration 035 aligned provider CHECK to ('google','microsoft'), saveTokens now throws on upsert error, callbacks try/catch with ?email_error= redirect, integrations page shows green/red banner from query params. Required env: NEXT_PUBLIC_APP_URL=https://jobtrackr-cv.vercel.app, GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET (Outlook optional). Google OAuth setup: redirect URI = ${NEXT_PUBLIC_APP_URL}/api/auth/email/google/callback, Gmail API enabled in Library, test users added to consent screen. Migrations 033/034/035 + cv-backend + worker + web deploys required (commands documented in commit messages). NEXT priorities: (E-3) per-profile daily_application_limit enforcement (Redis counter, default 10/day) — currently letters auto-generate with no cap; (E-4) UI notification when auto-analyze + auto-letter complete (banner: 'Auto-prepared N new applications'); (D-3) inline cover-letter editing in the outbox cards before send; (F-a) JD-regex email extraction during scraping to lift has_email from ~1% toward 20-30% (so more jobs flow into Ready to email rather than Pool→apply); (G) Analytics dashboard — funnel view scraped→analyzed→passed-gate→letter→sent→applied per profile/source; (H) Realtime notifications via Supabase channels. My recommendation: F-a next — half-session of regex work on worker, multiplies the value of every later phase by populating contact_email automatically; OR E-3 if you've seen automation produce too many letters.

Earlier next_action: Phase A + B + C (1/2/3) + D (1+2) + E-1 CODE DONE (2026-05-21). Auto-analyze is wired into the worker scrape pipeline: when a scrape completes for a profile with automation_enabled=true, every newly-saved job runs through cv-backend /internal/analyze with automation:true (after thin-JD pre-check + idempotency guard). cv-backend's existing C-3 initial-gate stops low-match jobs before tailoring; the `automation` column on analysis_runs records auto vs manual provenance. Cover-letter generation stays user-triggered (deferred to E-2 per the 'user verifies before send' principle). ** TWO Fly deploys required ** before E-1 takes effect: (1) `cd cv-backend && flyctl deploy -a jobtrackr-cv-api` (adds automation field to AnalyzeRequest), (2) `cd worker && flyctl deploy -a jobtrackr-cv-worker` (adds autoAnalyzeBatch). To test: edit a profile, toggle Pipeline automation ON, then click Run now (or wait for scheduled). The scrape will complete then auto-analyze fires for any new jobs. After deploy you can verify with: SELECT count(*) FILTER (WHERE automation=true) AS auto_runs, count(*) FILTER (WHERE automation=false) AS manual_runs FROM analysis_runs WHERE created_at > now() - interval '1 hour'. NEXT priorities: (E-2) chain cover-letter generation after a successful auto-analysis that passed both gates — completes the 'scrape → letter ready' fully-automated flow; (E-3) per-profile daily_application_limit enforcement (Redis counter, default 10/day); (E-4) UI notification when auto-analyze completes (banner: 'Auto-analyzed N new jobs · M ready to apply'); (D-3) inline letter editing in the outbox cards; (F) email-send pipeline. My recommendation: E-2 next — it closes the automation story end-to-end.

Earlier next_action: Phase A + B + C (1/2/3) + D-1 + D-2 DONE (2026-05-21). Final-ATS gate now blocks the cover-letter generation endpoint when passed_final_gate=false, mirroring C-3's initial-gate pattern. Web-only change (cv-backend stays oblivious — gate is a cheap boolean read before the variants AI call). Override path: ?override=final_gate on POST /api/jobs/[id]/cover-letter. UI: amber 'Below your final-ATS threshold' card in CoverLetterPanel with score/threshold and 'Generate cover letter anyway' button that re-fires with override. NEXT priorities: (E) automation worker — schedule pipeline runs respecting automation_enabled per profile + apply gates with early-stopping in background; (F-a) JD-regex email extraction during scraping to lift has_email from 1/141 toward ~20-30% — small high-value win; (F-b) Gmail OAuth send pipeline (the actual outbox 'Send' button); (D-3) inline letter editing + copy/download from outbox cards. My recommendation: F-a next — half-session of regex work on the worker side, multiplies the value of every later phase by making the email channel actually populated.

Earlier next_action: Phase A + B + C (1/2/3) + D-1 DONE (2026-05-21). The Applications outbox is live at /dashboard/applications with 4 lifecycle sub-tabs (Ready to email / Ready to apply / Sent / Archived), rich per-row cards (job + tailored score + letter preview + channel ribbon + actions), and a new Send icon under Overview in the sidebar. Reads directly from cover_letters JOIN jobs JOIN analysis_runs JOIN search_profiles — no applications table writes yet (deferred to Phase F when email send becomes real). Mark-applied / Archive use the existing job actions; revalidate-path now also hits /dashboard/applications so the bucket counts stay fresh. NEXT priorities (you can pick): (D-2) cv-backend final-ATS gate enforcement on cover-letter generation endpoint with override path — mirrors the C-3 initial-gate pattern, keeps cost discipline on the cover-letter call; (D-3) inline letter editing + 'copy letter' button; (E) automated worker that schedules pipeline runs respecting automation_enabled per profile + applies the gates with early-stopping (currently gates only fire on user-triggered Analyze); (F-a) JD-regex email extraction during scraping (lifts has_email from 1/141 toward ~20-30%); (F-b) Gmail OAuth send pipeline (the actual outbox 'Send' button). My recommendation: D-2 next — small change, same pattern as C-3, closes the gate story before we move to the bigger E/F work.

---

## Completed Phase Narratives

### Entry 0

Phase 0 — preview env verified

### Entry 1

Phase 1 — migrations 010/011/012 applied; tables live + Realtime publication active

### Entry 2

Phase 2 — cv-backend deployed; /health 200, signed /internal/analyze returns 202

### Entry 3

Phase 3 — CV upload UI + flow working for PDF (DOCX has known issue BUG-1, parked)

### Entry 4

Phase 4 — BYOK keys (Anthropic/OpenAI/DeepSeek) UI + routes

### Entry 5

Phase 5 — Analyze button wired end-to-end; JD analysis step runs live (polling fallback added because Realtime delivery is unreliable)

### Entry 6

Phase 5.5 — Manual JD edit + contact email

### Entry 7

Phase 6 — Wire remaining steps + result cards

### Entry 8

Phase 7 — Tailored CV PDF + viewer (Download PDF button via pre-rendered ReportLab)

### Entry 9

Phase 7.5 — Profile settings (contact details + portfolio projects)

### Entry 10

Phase 8 — Analyses tab + history view grouped by job

### Entry 11

Phase 8.5 — Integrations page consolidated (AI keys merged in); separate AI-keys nav removed

### Entry 12

Phase 8.6 — cv-magic detail layout parity: CvJdMatchingCard table + ATS named sub-scores + 4-bucket FeasibilityCard with Evidence/Suggested rewrite + Tailored ATS-lift circle layout + Print/.md/PDF download trio on TailoredCvCard

### Entry 13

Phase 9.1 — 5-theme system: Default / Classic / Gilded Noir / Notion / Clay. ThemeProvider + FOUC guard + CSS variable token architecture. Theme picker in sidebar.

### Entry 14

Phase 9.2 — Sidebar redesigned to match cv-magic reference: brand-colour left-bar active indicator, lucide icons, per-theme font/radius/width tokens.

### Entry 15

Phase 9.3 — All Tools pages (CV library, Analyses, Profile/Settings, Integrations, Analysis run) ported to cv-magic design language: glass cards, label-luxury, page-title/subtitle typography.

### Entry 16

Phase 9.4 — Job table: theme-aware hover, row actions collapsed to Analyze + tri-dot (⋮) dropdown (Edit JD, View analysis, Mark applied, Dismiss). Edited JD label green.

### Entry 17

Phase 9.5 — Job filter bar refactored: JobStatusTabs on own row + unified JobFilterBar (secondary filters + sort) on single nowrap row. Inline styles override .field{width:100%} bug.

### Entry 18

Phase 9.6 — ProviderPicker accordion replaces AiKeyCard on Integrations page. Each card: radio (preferred), key input/connect/replace/disconnect, model pills (PATCH to DB). Preferred provider → localStorage; AnalyzeJobButton reads it + sends as body param; API honours override or falls back to priority.

### Entry 19

Phase 9.7 — Theme-aware scrollbar: thumb uses color-mix(brand 35%) so each theme renders its own colour.

### Entry 20

Phase 9.8 — Global hex color sweep: replaced all hardcoded #D0D7DE/border, #F6F8FA/surface-2, #0969DA/brand, #1F2328/text etc. with CSS var() tokens across 21 files so all elements respond to theme switches.

### Entry 21

Phase 9.9 — ⋮ RowMenu dropdown fixed: createPortal + position:fixed so menu escapes overflow:hidden table wrapper and animation containers. All options now visible.

### Entry 22

Phase 9.10 — Soft Skills layout fixed: SkillRow changed from flex-wrap (label+chips on same line) to flex-col; label now always on own line consistent with Technical/Domain Knowledge.

### Entry 23

Phase 9.11 — Apply/Dismiss reliability: switched from startTransition (silent error swallow) to direct await with try/catch + UI revert on server action failure.

### Entry 24

Phase 9.12 — CV re-categorise: added '↺ Categorise skills now' button to CvSkillsBlock + new POST /api/cv/[id]/recategorise route; auto-retries with provider default model if stored model fails.

### Entry 25

Phase 9.13 — Sign out fixed: middleware was intercepting POST /auth/signout and redirecting to /dashboard because /auth/* rule had no exclusion for /auth/signout. Added exclusion. Also added visible 'Sign out' text label next to icon.

### Entry 26

Phase 9.14 — Analysis history shows final (tailored) score; bare percentage with initial in grey parens when both exist.

### Entry 27

Phase 9.15 — Active CV sorts to top of list immediately when Set Active clicked.

### Entry 28

Phase 9.16 — OpenAI non-chat model fallback: cv-backend client.py detects 'not a chat model' error and retries with gpt-4o. Web routes also retry with ai_model=null on failure.

### Entry 29

Phase 9.17 — Brand-tinted theme borders: Clay --border #F5BECE (pink), Notion --border #C1B7EC (purple), Gilded Noir --border #5C4D23 (gold-dark). Table lines, card outlines, form boxes now match each theme's brand.

### Entry 30

Phase 9.18 — ProviderPicker OpenAI models updated to real chat models: GPT-5.1 (recommended), GPT-4o, GPT-4o mini, GPT-4 Turbo, o3-mini. Removed fictional gpt-5.2/gpt-5.2-pro/gpt-5-mini.

### Entry 31

Session A cleanup (2026-05-16) — Pre-cover-letter hygiene: (1) Migration renames: 005_vertical_filter→006_vertical_filter, 009_possible_duplicate→009_add_possible_duplicate_dedup_status. No prod impact — migrations are CLI-untracked, applied manually via SQL editor. (2) Dead components deleted: AiKeyCard.tsx (superseded by ProviderPicker), JobCard.tsx, JobSortBar.tsx (both superseded by JobTable/JobFilterBar in Phase 9), ToggleActiveButton.tsx (never wired up). (3) scrapeJobUrl.ts kept but marked as DEFERRED-1 in deferred_features; deferred comment block added to file header. (4) bullmq + ioredis confirmed legitimate — server-side only, used by job-discovery pipeline queue trigger in run/route.ts and actions.ts; do not remove. (5) Test artefacts removed from git: 2× Jora CSV, discovered_slugs.json, jora_scraper.py, verify_slugs.py, 3× jora_test JSON, test-update.js, fix-dead-links.js. Patterns added to .gitignore with explanatory comment.

### Entry 32

Session B orchestration layer (2026-05-16) — Permanent subagent + slash command infrastructure. (1) .claude/agents/ created: planner (haiku-4-5, Read/Grep/Glob, plan-only), auditor (opus-4-7, Read/Grep/Glob/Bash read-only, PASS/FAIL structured review), migration-checker (sonnet-4-6, Read/Grep/Glob/Bash, pre-migration safety checker). (2) .claude/commands/ created: /plan (invokes planner, stops for approval), /audit (invokes auditor, halts on FAIL), /ship (graph.json → commit → push → summary ritual), /handoff (generates copy-pasteable next-session block). (3) CLAUDE.md updated: Model Routing section (routing table + Opus-is-reviewer rule), Session Management Rules section (60% threshold + /handoff protocol). (4) Smoke test passed: planner and auditor both verified to run in isolated agent contexts with correct models, producing structured output.

### Entry 33

Session C prompts.py modular split (2026-05-17) — Split cv-backend/app/services/ai/prompts.py (1413 lines, 12 constants) into a prompts/ package: 6 step modules (jd_analysis.py, cv_jd_matching.py, keyword_feasibility.py, ai_recommendations.py, tailored_cv.py, cv_skill_categorisation.py) + __init__.py with backward-compatible re-exports. All 6 consumer import paths unchanged. All 5 PLAN-1 auditor corrections applied (exact line boundaries, skill_categoriser consumer included, atomic commit, local-only verification, graph.json in same commit). Three smoke tests passed. Session required graceful mid-session recovery when tailored_cv.py (~970 lines) exceeded executor context capacity — OPS-4 heuristic captured.

### Entry 34

Session D documentation pass (2026-05-17) — Created docs/architecture.md (tech stack, data flow, HMAC boundary, BYOK, endpoints, deployment, constraints), docs/database.md (exhaustive schema reference: 10 tables, 2 storage buckets, functions, triggers, extensions — derived from all 20 migration files), and docs/cover-letter-spec.md (full 10-part cover letter generation spec, verbatim). Auditor returned PASS_WITH_NOTES; three fixes applied: HMAC encoding precision note in architecture.md, illustrative model-name disclaimer in cover-letter-spec.md Part 4, ai_provider/ai_model added to AnalysisRun.fields in graph.json. OPS-5 operational note captured. Primary criterion confirmed: a future planner can plan Phase 1 (Voice Fingerprint Module) from the three docs + graph.json alone.

### Entry 35

Phase 10.2.a — Story Extraction and Storage (2026-05-18) — Migration 022 (stories table: JSONB numbers, text[] tags, datetime extraction_timestamp with no DB default, composite index on (user_id, extraction_timestamp DESC), set_updated_at trigger, RLS users_own_stories). cv-backend: app/schemas/stories.py (StoryNumber, Story with datetime extraction_timestamp, ExtractStoriesResponse), app/services/stories/story_extractor.py (privacy boundary block, _MAX_CV_CHARS=12_000, full Pydantic validation per story, extraction_timestamp injected by extractor not model), app/services/ai/prompts/cover_letter/story_extraction.py (STORY_EXTRACTION_SYSTEM with 8 BAD/GOOD calibration pairs + empty-result instruction, STORY_EXTRACTION_USER_TEMPLATE), story_extraction_fixtures.py (RICH_CV/SPARSE_CV/GENERIC_CV with P1-P4 criteria + async run_fixture_check()), POST /internal/extract-stories endpoint (HMAC-protected, mirrors extract-voice-fingerprint pattern), prompts/__init__.py re-exports updated. Web: cvBackend.ts (Story/StoryNumber/ExtractStoriesPayload/ExtractStoriesResult interfaces + extractStories() wrapper, 90s timeout), POST /api/user/stories/extract route (session auth → active CV from cv_versions → BYOK key resolve → cv-backend call → atomic delete+single-statement INSERT). Auditor verdict: PASS_WITH_NOTES — 3 doc fixes applied post-audit (extraction_timestamp docstring corrected to datetime, SPARSE_CV P5 verb-ban criterion removed from prose and never in list, prose P-count now matches list at 4). Smoke test passed 2026-05-18 (commit 2bdb835): 8 stories extracted and persisted for test user — correct UUIDs, timestamps, domains, and years verified in Supabase Table Editor. Phase 10.2.a fully complete.

### Entry 36

Phase 10.3 — Company Research Pipeline (2026-05-18) — Migration 024 (company_research: text PK slug, jsonb facts/voice_signals/hiring_intel, research_quality_score float, search_skipped bool, auth.role()='authenticated' RLS — no user_id). cv-backend: app/schemas/company.py (CompanyFacts/VoiceSignals/HiringIntel/CompanyResearch Pydantic models + request/response schemas), app/services/company/slug.py (make_company_slug — deterministic slug, 80-char cap), app/services/company/quality_scorer.py (compute_quality_score — 3-signal formula, no AI), app/services/company/researcher.py (Tavily search + httpx scrape + AI distillation + stale-flag injection on RecentEvent), app/services/company/fact_selector.py (keyword scoring, story_matcher pattern, no AI), app/services/ai/prompts/cover_letter/company_research.py (bad/good fact framing prompt). TAVILY_API_KEY added to config.py + .env.example. POST /internal/research-company + POST /internal/select-company-fact added to internal.py. Web: cvBackend.ts (ResearchCompany* + SelectCompanyFact* typed wrappers, 120s/10s timeouts), POST /api/company-research (trigger+cache), GET+POST /api/company-research/[company_id] (read+refresh), POST /api/company-research/facts/select (fact ranking). Auditor PASS_WITH_NOTES: two post-audit fixes applied (dead TTL_SECONDS constant removed, Exception narrowed to AIClientError). Smoke tests C/D/E all passed: completed+skipped=true, cached on 2nd call, ranked facts returned deterministically. Phase 10.3 fully complete. Session I post-merge fixes (commit 8882fc4): /api/company-research auth+DB lookup parallelised with Promise.all (cache-hit latency ~600ms faster); /api/company-research/facts/select refactored to accept {job_id} and resolve company_id/jd_text/cv_text server-side (ownership: jobs→search_profiles→user_id, JD: manual_jd_text→analysis_run fallback→422, CV: cv_versions is_active=true).

### Entry 37

Phase 10.2.b — Story Matching and UI (2026-05-18) — Migration 023 (replace_stories RPC: plpgsql SECURITY DEFINER function wrapping DELETE+INSERT in one transaction, eliminating the Phase 10.2.a DELETE-INSERT gap; service_role-only execute grant). cv-backend: app/services/stories/story_matcher.py (deterministic keyword scoring: frozenset tokenisation, ~150 English function-word stop-words, overlap normalised by JD size, +0.15 numbers bonus, capped at 1.0, sorted descending), app/schemas/stories.py extended (id: Optional[str]=None added to Story, MatchStoriesRequest/ScoredStory/MatchStoriesResponse added), POST /internal/match-stories endpoint (no AI call; logs jd_len+story count only). Web: cvBackend.ts (MatchStoriesStory/MatchStoriesPayload/ScoredStory/MatchStoriesResult interfaces + matchStories() 10s wrapper), GET /api/user/stories (two-query batch: max timestamp → batch), POST /api/user/stories/match (job ownership chain → JD resolution: manual_jd_text → analysis_run fallback → 422 → story batch → cv-backend → Map score merge → sorted response), PATCH /api/user/stories/[id] (ownership check + tag/one_line constraints enforced), extract/route.ts updated to call rpc('replace_stories',...) replacing two-call pattern. UI: StoriesClient.tsx (335 lines: StoryCard sub-component, multi-expand Set<string>, inline tag editing state machine, 90s re-extract in-flight label, state-update-in-place on save/re-extract), voice/page.tsx extended (server-side two-query story fetch, StoriesClient rendered below VoiceCaptureClient with hr divider). Auditor: initial FAIL (MatchStoriesStory missing title+year — Pydantic 422 on every match call); both fixes applied; re-verified manually; TypeScript clean. Phase 10.2.b fully complete.

### Entry 38

Session E — Phase 10.1 (Voice Fingerprint Module, 2026-05-18) — Migration 021 (voice_profiles: RLS, unique user_id, updated_at trigger) applied manually. cv-backend: app/schemas/voice.py (VoiceFingerprint Pydantic model, 14 fields with type constraints), app/services/voice/trust_scorer.py (pure function, TrustScore NamedTuple, 23 AI-tell phrases, deterministic), app/services/voice/trust_scorer_fixtures.py (6 smoke-test fixtures), app/services/voice/voice_fingerprint.py (extract_voice_fingerprint, full Pydantic validation, server-side-only error logging, no_training=True), app/services/ai/prompts/cover_letter/ (new subdirectory: voice_fingerprint.py prompt), app/services/ai/client.py (no_training: bool = False threaded through complete_json → complete → _anthropic_complete/_openai_complete; OpenAI store=False implemented; Anthropic TODO pending SDK support), app/routes/internal.py (POST /internal/extract-voice-fingerprint). Web: web/src/lib/cvBackend.ts (extractVoiceFingerprint wrapper), web/src/app/api/user/voice-profile/route.ts (GET omits voice_sample_raw; POST: key lookup/decrypt/call/upsert; TODO Phase 1.5 trust regression warning), web/src/components/cv/VoiceCaptureClient.tsx (paste disabled, word counter, TrustBadge), web/src/app/(dashboard)/dashboard/voice/page.tsx (Server Component), web/src/components/SidebarNav.tsx (Mic icon + My Voice link). Auditor verdict: PASS_WITH_NOTES — 20/21 checkpoints clean; one fixture calibration bug fixed post-audit (OBVIOUS_AI expected_length_score 1.0→0.7, ~165 words corrected to ~136 words). All 6 smoke-test fixtures PASS after fix. OPS-6 captured. cv-backend redeployed 2026-05-18 (commit c931c07 → image deployment-01KRW3TWARNA0PWS6PKZ4FF2PS, version 17, region syd). /internal/extract-voice-fingerprint confirmed live via OpenAPI spec check. Browser E2E smoke test (sign in → /dashboard/voice → submit sample → verify fingerprint) pending manual verification by user.

### Entry 39

Phase 10.4 — Cover Letter Generation (2026-05-18 + 2026-05-19) — Session 1: migration 025 (cover_letters table: status enum, generation_status JSONB, pass_1/2/3 text+model cols, gate scores, quality_flags JSONB, is_stale, RLS, Realtime publication), cv-backend three-pass pipeline (generator.py + model_router.py + quality_gates.py + 3 pass prompts + gate_1_honesty), /internal/generate-cover-letter endpoint. Session 2: web POST/GET routes, CoverLetterPanel UI with Realtime subscription, wired into analysis run page. Session J (refactor, 2026-05-19): replaced 3-pass pipeline with single-call architecture after end-to-end quality testing showed mechanical-feeling output. New design: one call to user's chosen model with voice sample + CV + story + company fact + four-paragraph intent rubric + anti-patterns; honesty gate rewritten for scope (candidate-claims only) and leniency (paraphrase tolerance). Company research is now a prerequisite (web POST returns 422 + action=research_company if missing; UI shows 'Research [Company]' button calling existing /api/company-research). Low-quality research falls back to JD-hook with warning. P1 spec bans role-first openers and demands substantive candidate-perspective claim (with 3 different-domain example shapes to prevent verbatim copy + formula entrenchment). P3 spec uses contrastive WEAK/STRONGER examples for structural variety + 4 named texture categories (colleague reaction / what changed afterwards / what was hard / specific moment). Deleted: model_router.py, quality_gates.py, pass_1/2/3_*.py prompts. DB schema unchanged; old rows keep 6-key generation_status, new rows use 2-key. quality_flags merge in generator preserves web-layer pre-writes. Commits 8044aa2, 07e3b2b, 0c3679f, fd42d47, f12732a.

### Entry 40

Phase 10.5 — Delivery Template + PDF Export (2026-05-19, Session K) — Initial ship (Haiku, commits 0c21b19→caefadf): Migration 026 (hiring_manager column on jobs), suburb/postcode added to user_preferences contact_details, JobEditModal hiring_manager field, /api/jobs/[id]/cover-letter/[letter_id]/{template,download} routes, CoverLetterPanel rewritten with jsPDF client-side rendering (A4, 0.8in margins, 11pt Helvetica). Post-ship Opus audit caught 5 real bugs that all shipped: (1) /template route was dead code, never wired; (2) ownership-fail returned 401 instead of 403; (3) hardcoded 'Australia' line in employer block; (4) 'M' user-specific fallback initial in filename slug; (5) job.location existed and was unfetched, so employer block was missing city/state. Three behavioural bugs also fixed: editedBody not reset on regenerate (silent stale-edit send), hiring-manager name duplicated in employer block + salutation, name positioned BELOW company instead of above (inverted AU convention). Plus two product-feedback fixes: dropped RE: line entirely (redundant in one-job letters), and company research now auto-triggers transparently with didAutoResearch loop guard (no manual 'Research [Company]' button). Cleanup commits: f45a3df (5 bug fixes + shared coverLetterTemplate.ts helper extracted, 122-line net reduction), 8f270e6 (hiring-manager dedup + reorder), 463aa0c (wire job.location), ec5f086 (drop RE: + auto-research). All pushed and Vercel-deployed. Final ship state: AU-standard format, single-page fit verified, accessible modal (role=dialog, Escape, backdrop click, autoFocus), shared template helper at web/src/lib/coverLetterTemplate.ts.

### Entry 41

Worker curl_cffi infrastructure + SEEK direct + Careerjet integration (2026-05-20) — curl_cffi (Chrome 124 TLS impersonation) bypasses Cloudflare from Fly.io datacenter IP without any proxy — confirmed in production. New files: scripts/fetch_jd.py (Python subprocess: Chrome 124 JA3/ALPN via curl_cffi, --proxy + --no-redirect flags, single JSON-line stdout), src/lib/curlfetch.ts (TypeScript wrapper: curlFetch() full-body fetch + curlRedirect() header-only probe). seekDirect.ts: replaced got-scraping with curlFetch; added normaliseSeekLocation() stripping ', Australia' suffix from state-level locations; 22 SEEK jobs/run at $0 from Fly datacenter without any proxy. careerjet.ts: Phase 1 — Careerjet API listings + immediate tracking-URL resolution (Node native fetch(redirect:'manual') at CONCURRENCY=5 resolves jobviewtrack.com/v2/<hash> → careerjet.com.au/clk/<hash> within seconds of API call, before tokens expire); Phase 2 — curl_cffi JD enrichment (10/11 full descriptions merged, 421–6846 chars each). Careerjet.com.au URLs stored in DB — links are permanent and clickable in UI. Apify actor path preserved as automatic orchestrator fallback — never ran. Dockerfile: python3-minimal + pip3 install curl_cffi + COPY scripts/fetch_jd.py. Production run confirmed: SEEK 22 jobs + Careerjet 20 jobs, Apify cost $0.

### Entry 42

Phase E-1 CODE — Worker auto-analyze on scrape (2026-05-21, two Fly deploys pending) — Wires the scrape pipeline to fire cv-backend /internal/analyze for every newly-saved job when the profile has automation_enabled=true. Cover-letter generation stays user-triggered (deferred to E-2 per 'user must verify before send'). Pipeline stages: scrape → save → (NEW) stage 13: autoAnalyzeBatch over savedIds → cv-backend /internal/analyze (HMAC-signed) → existing C-3 gates handle thin-JD-skip + below-initial-skip. The `automation` column on analysis_runs records run provenance (auto vs manual) — Phase E-2 and analytics can use this to distinguish. CHANGES: (cv-backend) schemas/internal.py AnalyzeRequest gains automation: bool = False; services/pipeline/orchestrator.py writes automation=True to the analysis_runs row at mark_run_running time when payload.automation=true. (web) lib/cvBackend.ts AnalyzePayload gains optional automation?: boolean — web /analyze route still passes false by default. (worker) pipeline/save.ts: SaveResult gains savedIds: string[] (collected from the .select('id') upsert). pipeline/orchestrator.ts: FullProfile extended with automation_enabled, min_initial_ats, min_final_ats; loadProfile SELECT extended; stage 13 added after saveJobs that calls autoAnalyzeBatch when automation_enabled=true. NEW worker/src/lib/cvBackendHmac.ts — port of web's callCvBackend with HMAC-SHA256 over (X-Timestamp + raw body) using shared JOBTRACKR_HMAC_SECRET. NEW worker/src/automation/triggerAutoAnalyze.ts — per-job logic mirroring the web /analyze route: fetches job; skips if jd_quality='thin' AND no manual_jd_text OR if description < 200 chars; idempotency check on existing non-stale analysis_runs (auto-analyze runs after EVERY scrape so without this guard re-scrapes would duplicate); fetches active CV + AI key (admin DB); decrypts via existing lib/crypto.ts decryptApiKey; marks prior non-stale runs stale (defensive); INSERTs new analysis_runs row with automation=true; fires cv-backend /internal/analyze with all gate thresholds + automation:true; failures logged and skipped (don't fail the scrape pipeline). autoAnalyzeBatch processes one job at a time to avoid bursting cv-backend / AI provider. Worker env requirements: JOBTRACKR_HMAC_SECRET + CV_BACKEND_URL (add to Fly secrets if not already there). Type-check passes for all three codebases (web, worker, cv-backend Python syntax). **DEPLOY ORDER**: cv-backend FIRST (so it accepts the new automation field), worker SECOND. After both deploys, toggle Pipeline automation ON for a profile and click Run now — newly-saved jobs auto-analyze.

### Entry 43

Phase D-2 — Final-ATS gate on cover-letter generation (2026-05-21) — Mirrors the C-3 initial-gate pattern but for the cover-letter route, closing the gate story. Pure web change — cv-backend unchanged, no Fly redeploy. /api/jobs/[id]/cover-letter parses ?override=final_gate|all from the URL; after the job ownership chain, reads the latest non-stale analysis_run.passed_final_gate; when === false AND no override, returns 422 with {action: 'below_final_gate', tailored_score, threshold} BEFORE the ~5-15s synchronous variants AI call. The threshold is read from search_profiles.min_final_ats (per-profile, not hardcoded). Null-gate case: when passed_final_gate is NULL (no analysis run yet, or pre-C-2 row), the gate does NOT block — strict `=== false` check, deliberately permissive when uncertain. CoverLetterPanel.tsx: new belowFinalGate state holding {score, threshold}; handleGenerate gains a third param `override?: 'final_gate'`; when set, the request URL gets ?override=final_gate appended; new 422 branch catches action='below_final_gate' and sets the state instead of showing the generic error; new amber 'Below your final-ATS threshold' card renders above the existing error block, showing the actual score/threshold and a 'Generate cover letter anyway' button that re-fires handleGenerate(true, false, 'final_gate'). Dismiss clears the state. Type-check + production build clean. Saves ~5-15s + cost of one AI call per low-tailored-score job that the user would otherwise discard. Phase E (automation worker) and Phase F (email integration) are the next big chunks.

### Entry 44

Phase D-1 — Applications outbox shell (2026-05-21) — New route at /dashboard/applications surfaces every job with a completed cover letter, bucketed into 4 lifecycle sub-tabs: Ready to email (has contact_email, not applied, not archived), Ready to apply (no email — open job link manually), Sent / Applied (job.applied_at not null), Archived (dismissed_at not null and not applied). Single source of truth: reads from cover_letters JOIN jobs JOIN analysis_runs JOIN search_profiles — no applications-table writes yet (deferred to Phase F when send pipeline becomes real; the table from Migration 031 sits idle for now). NEW files: components/applications/ApplicationStatusTabs.tsx (URL-param sub-tabs matching JobStatusTabs pattern; default tab 'email'); components/applications/ApplicationCard.tsx (richer card layout vs the JobTable row — title/company/score header, letter preview ~180 chars, channel ribbon 'To: <email>' or 'No email — open job link', actions row View full / Open job / Mark applied / Archive; uses existing markJobApplied + markJobDismissed server actions; smooth exit animation on bucket change); app/(dashboard)/dashboard/applications/page.tsx (server component — auth check, empty-state when zero letters, three-query stitch matching the dashboard pattern, JS-side bucket counts BEFORE filter so tab counts never collapse to 0, sub-tab filter applied last). EDITED: SidebarNav.tsx adds the Send-icon 'Applications' entry under Overview right after Dashboard; actions.ts revalidatePath additions so Mark-applied/Archive refresh the outbox bucket counts in addition to /dashboard and /dashboard/profiles/[id]/jobs. Type-check + production build clean. Build registers /dashboard/applications as a server-rendered dynamic route. Zero backend changes. On your data: this lists the 2 jobs that have completed cover letters (look for them in the appropriate buckets based on applied_at + has_email).

### Entry 45

Phase C-3 CODE — HARD gates for manual runs (2026-05-21, awaits Fly redeploy) — User direction: 'analysis should stop below ATS and not do any analysis for thin JD — even for manual'. C-2's record-only behaviour was therefore upgraded to hard early-stop with explicit overrides. NO new column added to analysis_runs; the orchestrator marks downstream step_status entries as 'skipped' so the UI can distinguish deliberate skips from pending/failed states. cv-backend changes: schemas/internal.py — AnalyzeRequest.skip_initial_gate: bool = False (forwarded by web when user clicks Force); services/pipeline/progress.py — StepState Literal extended with 'skipped'; services/pipeline/orchestrator.py — after recording passed_initial_gate, if False AND skip_initial_gate=False, mark input_recommendations/keyword_feasibility/ai_recommendations/tailored_cv as 'skipped' and mark_run_completed (saves ~3 AI calls per low-match job). Web changes: lib/cvBackend.ts — AnalyzePayload gains optional skip_initial_gate; src/app/api/jobs/[id]/analyze/route.ts — parses ?override=thin_jd|initial_gate|all from URL; thin-JD pre-check at API boundary (when job.jd_quality==='thin' AND no manual_jd_text AND no override → 422 with action='paste_jd', ZERO cv-backend call); forwards skip_initial_gate=(override==='initial_gate'||override==='all') to backend. UI changes: components/cv/AnalyzeJobButton.tsx — new optional 'override' and 'compact' props; compact mode renders an amber 'Force →' link with Zap icon; URL builds with ?override=… when override is set. components/jobs/JobTable.tsx — when pipelineState==='below_initial' renders a compact override AnalyzeJobButton inline beside the badge in the Role cell; same for pipelineState==='needs_jd' with override='thin_jd'. Override remains available at the API for both states via direct POST. Type-check + Python syntax-check pass. **Deploy required**: `cd cv-backend && flyctl deploy -a jobtrackr-cv-api`. Verification: thin-JD job analyze → 422 'Paste full JD'; score-18 job re-Analyze → step_status.tailored_cv='skipped' + no tailored_pdf_storage_path; click Force → on the badge → succeeds with full tailoring.

### Entry 46

Phase C-2 CODE — cv-backend gate recording (2026-05-21, NOT YET DEPLOYED TO FLY) — Wires the two ATS gates into the existing manual pipeline. RECORD-ONLY semantics: gates compute pass/fail booleans and write them to analysis_runs but the pipeline never stops early on manual runs (early-stopping arrives in Phase E with the automated worker). cv-backend/app/schemas/internal.py: AnalyzeRequest gains min_initial_ats: float = 55 and min_final_ats: float = 75 (defaults match Migration 031 column defaults; backward-compat with older callers). cv-backend/app/services/pipeline/orchestrator.py: after `await save_step_result(run_id, 'match_score', ...)` (line 94) — also writes initial_ats_score (mirrors match_score so Phase B UI doesn't have to know the synonym) and passed_initial_gate (boolean compare); after `await save_step_result(run_id, 'tailored_match_score', ...)` (line 189) — also writes passed_final_gate. Null-safe (skips the write when the score itself is null). automation column stays at its NOT NULL DEFAULT false — Phase E will flip it to true for runs triggered by the automated worker. web/src/lib/cvBackend.ts: AnalyzePayload gains optional min_initial_ats + min_final_ats. web/src/app/api/jobs/[id]/analyze/route.ts: search_profiles SELECT extended with min_initial_ats + min_final_ats; both forwarded in the startAnalysis() payload. Type-check passes, Python syntax-check passes. **DEPLOYMENT REQUIRED**: `cd cv-backend && flyctl deploy -a jobtrackr-cv-api`. After deploy, ONE manual Analyze re-run on a job populates the new columns. Existing analysis_runs rows continue to have null gate columns — Phase B's derivePipelineState handles null gracefully (falls back to non-gate states).

### Entry 47

Phase C-1 — Pipeline automation settings UI (2026-05-21) — Adds the configuration surface for the gates Phase C-2 will read. New form section on /dashboard/profiles/[id]/edit (and the create flow): a checkbox 'Pipeline automation' (binds to automation_enabled) that gates 5 dependent fields visible when on — Initial ATS threshold (number 0-100, default 55), Final ATS threshold (number 0-100, default 75), Strict role match (checkbox), Email sending mode (3-radio: never / after_review / auto), Daily application cap (number, default 10). Help text on every field explains cost savings (e.g. 'Saves 3 AI calls per job'). When the toggle is OFF, hidden inputs preserve the existing DB values so a no-op edit doesn't clobber config. actions.ts: new extractAutomationFields() helper with clampInt() bounds validation matching the Migration 031 CHECK constraints (0-100 for ats thresholds, 0-1000 for daily limit, enum validation for auto_send_emails); spread into both createProfile and updateProfile. ProfileForm.tsx: extended Props.defaults with the 6 new fields, new useState<boolean>(defaults?.automation_enabled ?? false) for show/hide, new <div> block after the existing 'Automation mode' (scrape schedule) block — visually distinct so users know 'Automation mode' is about WHEN to scrape vs 'Pipeline automation' is about WHAT to do after scraping. Edit page: extended profile type cast + passed all 6 new defaults. NO cv-backend changes — gates don't fire yet. Default state for all 5 existing profiles: automation_enabled=false, thresholds at defaults. Type-check passes, production build clean.

### Entry 48

Phase B — Visibility UI for the new schema (2026-05-21) — Surfaces Phase A's columns on the dashboard + per-profile boards. NEW files: components/jobs/pipelineState.ts (pure deriver — derives one of {archived, applied, ready_to_send, ready_to_apply, below_final, below_initial, analysing, role_mismatch, needs_jd, discovered} from job + latestRun + latestLetter using a fixed precedence; PIPELINE_STATE_META + TONE_CLASSES for labels/colours/dots; same pattern as progressFlags.deriveProgress) and components/jobs/TriageBanner.tsx (one row at top of board listing actionable counts: 'N jobs need a JD pasted', 'N flagged as role mismatch', 'N auto-skipped by ATS threshold' — each with a Show → link that applies the corresponding chip; dismissable per browser-SESSION via sessionStorage so triage signals never get permanently silenced). EDITED: progressFlags.ts AnalysisRunRef extended with the 4 Phase A gate columns (initial_ats_score, passed_initial_gate, passed_final_gate, automation) as optional; JobTable Job interface gains optional jd_quality/role_match/has_email/pipelineState; JobTable renders a coloured State pill in the top row of the Role cell (only when state is meaningful — discovered has showAsBadge:false), plus an inline JD-quality icon next to location (FileWarning amber 'thin JD' label for thin, subtle FileQuestion for unknown, hidden for rich); JobProgressChips gains 4 new chips (Needs JD, Role mismatch, Has email, Auto-skipped) under a 'Triage' label separated from the 'Progress' group by a centered dot; JobFilterBar SORT_OPTIONS gains 'Rich JD first'; dashboard/page.tsx and profiles/[id]/jobs/page.tsx both: extend SELECT with jd_quality/role_match/has_email + analysis_runs gate columns, derive pipelineState alongside progress, compute 7-field chip counts BEFORE chip filter (so toggling never collapses to 0), extend chip filter with the 4 new keys, add JS-side rich_jd_first sort (CASE-style rank: rich=1/unknown=2/thin=3/null=4, tiebreak by posted_at DESC), mount TriageBanner between JobProgressChips and ContinueRail. ZERO pipeline changes, ZERO AI calls, ZERO new server actions. Built clean, type-check passes. On your data this will show: 41 'needs_jd' badges (thin JDs), 0 'role_mismatch' (SQL backfill only does match/uncertain), 1 'has_email' chip count, 0 'auto_skipped' (gate columns null until Phase C). Triage banner will pop with '41 jobs need a JD pasted' on first visit per session.

### Entry 49

Phase A — Migration 031+032 applied + verified (2026-05-21) — Schema foundation for the gated-pipeline overhaul. SCOPE: pure schema, zero code changes, existing flows unchanged. ADDED: jobs.jd_quality (CHECK 'rich'|'thin'|'unknown'), jobs.role_match (CHECK 'match'|'mismatch'|'uncertain'), jobs.has_email (GENERATED ALWAYS AS contact_email IS NOT NULL STORED — auto-computed, can be indexed); analysis_runs.initial_ats_score numeric, .passed_initial_gate boolean, .passed_final_gate boolean, .automation boolean NOT NULL DEFAULT false; search_profiles.automation_enabled bool DEFAULT false, .min_initial_ats numeric DEFAULT 55 (CHECK 0-100), .min_final_ats numeric DEFAULT 75 (CHECK 0-100), .role_match_strict bool DEFAULT false, .auto_send_emails text DEFAULT 'never' (CHECK 'never'|'after_review'|'auto'), .daily_application_limit int DEFAULT 10; cover_letters.auto_selected_variant_id text. NEW TABLES: applications (id/user_id/job_id/analysis_run_id/cover_letter_id/channel('email'|'apply_link')/email_draft/email_subject/status('draft'|'pending_review'|'queued'|'sent'|'failed'|'applied'|'archived')/sent_at/sent_to/error_message/user_verified/timestamps — RLS users_own_applications + set_updated_at trigger + 2 indexes (user_id+status+created_at DESC; job_id) + added to supabase_realtime publication) and email_integrations (user_id PK/provider/oauth_token bytea/from_address/timestamps — RLS only, Phase F schema stub with no code). BACKFILL (032): classified all 141 existing jobs — jd_quality 37 rich / 41 thin / 63 unknown; role_match 74 match / 67 uncertain (SQL does only match/uncertain — 'mismatch' deferred to JS denylist). KEY FINDING from has_email count: only 1/141 jobs has a contact_email in the existing pool. This means the apply_link channel (open job URL manually) will dominate; Phase F priority may shift toward JD-regex email extraction (cheap) or a manual email-paste UX before Gmail OAuth send becomes high-value. DESIGN DECISION: dropped pipeline_state + pipeline_blocked_reason columns from the original plan — state is derived in JS via a pure function (same pattern as components/jobs/progressFlags.deriveProgress), single source of truth, no trigger maintenance burden, no consistency risk. All 5 search_profiles received automation defaults. Phase B (visibility-only UI: State + JD-quality columns, chips, dashboard banner) is the next step — no pipeline changes, no AI changes, no automation logic.

### Entry 50

Phase 11 — Voice-directed drafting: multi-variant opening paragraphs (2026-05-20) — cv-backend: opening_variants.py (VARIANTS_SYSTEM + VARIANTS_USER_TEMPLATE with 4 named patterns: Story Hook, Conviction Statement, Mirror-Back, Before/After Contrast; Pattern D key constraint: contrast must be in thinking/method not metric results); schemas/cover_letter.py (OpeningVariant, GenerateOpeningVariantsRequest/Response, chosen_opening optional on GenerateCoverLetterRequest); services/cover_letter/variants.py (generate_opening_variants: single JSON-array call, temperature 1.0 for gpt-5* else 0.9, 700 max tokens, validates ≥3 variants); generator.py (_generate_body_with_chosen_opener using SYSTEM_BODY_ONLY + USER_TEMPLATE_BODY_ONLY; coherence constraint: P2 must build on P1's frame; run_cover_letter_pipeline branches on chosen_opening present/absent); /internal/generate-opening-variants endpoint (synchronous HTTP 200, HMAC-protected). Migration 027: adds 'picking' to status CHECK (DROP+ADD constraint pattern — text not enum); adds opening_variants/chosen_opening/discarded_openings columns. Web: cvBackend.ts (OpeningVariant/GenerateOpeningVariants* typed wrappers, 60s timeout); /cover-letter POST phase-split (variants generated before DB insert — failure returns 502 without leaving stuck rows; row created with status='picking'; returns {letter_id, status:'picking', variants}); /cover-letter/[letter_id]/pick POST (new route: auth → PickLetter cast workaround for Supabase type lag → 409 guard → find chosen/discarded → re-resolve prerequisites → PATCH to 'pending' → generateCoverLetter(chosen_opening:) → 502 rolls back to 'picking'; audit fix d61c048: company_name from jobs.company not cover_letters.company_hook_text); GET route updated to include new columns in SELECT. CoverLetterPanel: OpeningVariant interface, extended CoverLetterRow ('picking' in status union + 3 nullable fields), polling pauses during 'picking', handlePick(), stacked-card VariantPicker UI (pattern_label in small caps, 'Use this opener' button, disabled/loading during in-flight). Opus audit: PASS_WITH_NOTES — one blocking fix applied (d61c048), three non-blocking notes deferred. Commits: 8dbcf8c (11b web), d61c048 (audit fix).

### Entry 51

Phase E-2 — Auto cover letter after passing automation run (2026-05-22, commit 7eeeb24) — Closes the automation loop one step further. cv-backend orchestrator.py: after mark_run_completed, when payload.automation && final_score >= min_final_ats, fires asyncio.create_task(auto_generate_cover_letter(...)). NEW app/services/automation/auto_cover_letter.py (~170 lines, best-effort — never raises): (1) looks up job_id from analysis_runs by run_id; (2) idempotency check on cover_letters (skip if a non-stale letter already exists for the job); (3) fetches voice_profiles (fingerprint + voice_sample_raw — required, skips silently if missing); (4) picks most recent story (no story-matching API call — automation uses best available); (5) company hook from company_research_facts (or generic '"I've followed {company}'s work closely"' fallback); (6) inserts cover_letters row with status='generating', analysis_run_id=run_id; (7) fires run_cover_letter_pipeline as a separate asyncio.create_task so the orchestrator's response isn't blocked. All exceptions are caught and logged — never propagate to the parent pipeline. Required: voice profile must exist + at least one story must be extracted before automation will produce letters.

### Entry 52

D-1 revision — Applications pool with email-decision gate (2026-05-22, commits 7eeeb24 + 55f3345) — Original D-1 had three sub-tabs (Ready to email / Ready to apply / Sent / Archived) keyed off job.has_email at letter-completion time. Problem: automation produces letters for jobs the user has never seen — the channel decision should be the USER's, not derived from whatever scraping found. New design: NEW 'To review' (pool) default tab where letters land first; user must decide channel before the row moves on. NEW Migration 033: jobs.pool_decision_at timestamptz DEFAULT NULL — NULL=pool, SET=user decided. Three-state bucket: isPool (decision_at IS NULL), isEmail (decision_at NOT NULL + contact_email NOT NULL), isApply (decision_at NOT NULL + contact_email NULL). UI: ApplicationStatusTabs adds 'pool' as first tab labelled 'To review' (default URL); ApplicationCard renders amber decision box with email input in pool mode — Add email button (when input filled) writes contact_email + stamps pool_decision_at → moves to email tab; No email button (empty input) stamps pool_decision_at only → moves to apply tab. NEW server action markPoolDecision(jobId, profileId, email?) → user-scope client (RLS-protected) → revalidate /dashboard/applications + per-profile board. BUG fixed in 55f3345: jobs.has_email is GENERATED ALWAYS AS (contact_email IS NOT NULL) — Postgres rejects manual writes to it with 42501. Initial markPoolDecision wrote patch.has_email=true → every 'Add email' click crashed silently. Fix: only write contact_email; generation expression auto-updates has_email. Two related UX bugs also fixed: setTimeout-hide ran regardless of action outcome (card vanished then reappeared on error → now awaits success, hides only on success, surfaces errors inline via shared actionError state); stale TAB_HELP for email tab said 'download CV manually then send via email client' — pre-Phase-F copy — now describes Send button.

### Entry 53

Phase G — Cover letter PDF persistence + email attachment (2026-05-22, commit 863b698) — Closes the gap left by Phase F: cover letters were only stored as text (pass_3_final) and rendered to PDF client-side via jsPDF on demand. Phase F's send-email therefore only attached the tailored CV PDF; the cover letter went out as email body. Phase G stores the PDF server-side so it can be attached AND re-downloaded later. Migration 036: cover_letters.pdf_storage_path text + NEW Storage bucket 'cover-letters' (5 MB cap, PDF-only) + RLS owner-select policy (auth.uid() = first folder segment). NEW web/src/lib/coverLetterPdf.ts: renderCoverLetterPdf(text) → Buffer using jsPDF in Node — works because basic text+layout operations are pure JS, no canvas/browser deps. Layout MIRRORS the existing client-side render in CoverLetterPanel.handleDownloadPDF (A4 portrait, 0.8in margins, 11pt Helvetica, 1.35 line height, paragraph gaps on blank source lines) so stored PDFs match what users previously downloaded. NEW web/src/lib/coverLetterPdfStore.ts: ensureCoverLetterPdf(letterId, userId) → {path, bytes} — idempotent: returns existing bytes when pdf_storage_path is set AND Storage object exists, otherwise re-renders. Fetches letter + job + user_preferences, calls assembleLetter() (existing helper from coverLetterTemplate.ts), renders, uploads to '{userId}/{letterId}.pdf', patches column. Email helpers gmail.ts/outlook.ts: single optional attachment → attachments[] array (EmailAttachment/OutlookAttachment types). send-email route now calls ensureCoverLetterPdf (best-effort — non-fatal) and attaches BOTH CoverLetter_<company>.pdf + TailoredCV_<company>.pdf. NEW route /api/applications/[letter_id]/cover-letter-pdf: lazy-generates if needed, returns the PDF with Content-Disposition: attachment for browser download. ApplicationCard gains a 'Letter' Download button beside Open job on every tab (so users can fetch the letter that went out with a previous email from the Sent tab). Doesn't break existing CoverLetterPanel jsPDF flow — that still works; can be migrated later (G-2 in next_action).

### Entry 54

Phase H-1 — Pool count badge on Applications sidebar item (2026-05-22, commit 4dca9eb) — Visibility for the automation pipeline. Before H-1 the only signal that auto-cover-letter (E-2) had produced new letters was opening /dashboard/applications and looking at the To-review tab; you could miss work entirely. NOW: (dashboard)/layout.tsx counts cover_letters where status='completed' AND is_stale=false AND user_id = auth.uid() AND the joined jobs row has all three of pool_decision_at/applied_at/dismissed_at NULL. PostgREST !inner join with count:'exact' + head:true so the query returns just a number — no row payload. SidebarNav extended with optional poolCount prop, rendered via the existing NavItem badge slot (same brand-coloured pill as profile newCount). Renders only when > 0 so steady-state is visually quiet. Refreshes naturally via the existing revalidatePath('/dashboard/applications') calls in actions.ts since the layout re-runs on every navigation.

### Entry 55

Bug-fix pass — three real bugs surfaced during user testing (2026-05-22, commits 1de2106 + adc078b + 45431ed). (1) Migration 037 — cover_letters.analysis_run_id was missing. Phase E-2 auto_cover_letter.py inserted that column but migration 025 never created it (the column was added only to the new applications table in 031). Every E-2 attempt crashed with PG 42703, silently swallowed by the broad try/except. Diagnostic SELECT revealed it. Fix: nullable FK to analysis_runs with ON DELETE SET NULL + partial index. (2) Commit adc078b — jobs has NO direct user_id column. Ownership goes through jobs.profile_id → search_profiles.user_id (since migration 001). Two new Phase F/G routes filtered jobs with .eq('user_id', user.id) which returned null with no error, surfacing as 'job lookup failed' (cover-letter-pdf route) and 'Job has no contact email' (send-email route even when To: was clearly displayed). Fix: drop the bad filter from both routes; cover_letters.user_id check one step earlier remains the authoritative ownership gate. Status: code shipped, deploy + user-verification of Letter download + Send email still pending. (3) Migration 038 — jd_quality auto-classification. Migration 032 was a ONE-OFF backfill; jobs scraped after it stayed at jd_quality=NULL, so 'Needs JD' chip / TriageBanner / pipelineState='needs_jd' silently ignored every new job. AND the 032 threshold ('thin' < 300 chars) didn't match Phase E-1's worker skip threshold (2000), so 500-char teasers were 'unknown' per schema but auto-skipped by worker — user saw 5 Sydney jobs with NO ui signal explaining why automation didn't fire on them. Symptom: stage 13 log showed 'auto-analyze 5 jobs / triggered 0, skipped 5' but no UI surfaced 'thin JD'. Fix: classify_jd_quality() SQL function (single source of truth, 2000-char threshold matching worker JD_MIN_USABLE) + BEFORE INSERT/UPDATE trigger on jobs.{description,manual_jd_text} + full re-classify backfill. After 038 the 5 Sydney jobs reclassify NULL→'thin' and existing UI surfaces them everywhere expected.

### Entry 56

Phase H-3 — Bulk email send in Ready-to-email tab (2026-05-22, commit 6df6d69) — Confirms and documents the send model: user-initiated only, no auto-send code path. Adds batch capability so a curated set of emails can go in one go. NEW components/applications/EmailBulkBar.tsx wraps the email tab — selection checkboxes per card (same pattern as PoolBulkBar), Select all (N) / Deselect all toggle, sticky 'Send N emails' button when ≥1 selected. Confirmation modal lists every recipient (job title + company + contact email) before dispatch — irreversible action gets real confirmation, not a one-click. Sequential dispatch (await each fetch before next): Gmail API quota is ~250 units/sec/user, each send uses ~100 units; sequential is safe for batches up to ~30 and lets the UI hide each card immediately on success. Per-card status pill during/after batch: Sending (spinner with badge) / Failed (red badge with error message inline). Progress in the bulk bar: 'Sending 3 of 7…'. Batch-complete summary card when all rows clear with sent/failed counts + per-letter error list. Individual per-card Send buttons still work — bulk is purely additive. TAB_HELP for the email tab updated to mention the bulk option AND the no-auto-send guarantee. Other tabs unchanged.

### Entry 57

Phase D-3 — Inline cover letter editing (2026-05-22, commit c938c33) — Lets the user tweak a sentence in the letter body before sending without leaving the Applications page. Editing also invalidates the cached PDF so the next Send-email or Letter download re-renders from the new text. NEW /api/applications/[letter_id]/route.ts: GET returns full pass_3_final + email_sent_at + completed_at for the modal to prefill (list view only carries truncated preview); PATCH validates ownership, blocks edits to already-sent letters (409), enforces min/max length (50 / 20_000 chars), updates pass_3_final and clears pdf_storage_path so ensureCoverLetterPdf re-renders on the next request — the stale Storage object is overwritten by the next upsert rather than explicitly deleted. NEW components/applications/EditLetterModal.tsx lazy-loads the full body on open, textarea with char count + unsaved-changes indicator, Escape/backdrop-click close (unless saving), Save → PATCH → router.refresh() so the card's truncated preview updates from server data. No-op skip when text matches original. Pre-flight warning if email_sent_at is set. ApplicationCard gains a Pencil 'Edit' button next to Letter download — visible on all non-applied/non-archived cards (server blocks edits to sent letters; hiding the button avoids inviting the 409).

### Entry 58

Phase G-2 — Single PDF renderer for both outbox and CoverLetterPanel (2026-05-22, commit 9354a4c) — Before: two PDF renderers in the codebase — server-side jsPDF in lib/coverLetterPdf.ts (Phase G, used by Applications outbox + send-email) and duplicate client-side jsPDF inside CoverLetterPanel.handleDownloadPDF. Identical layout constants but two sites; any future tweak would have needed synchronised edits. Now: extended /api/jobs/[id]/cover-letter/[letter_id]/download with ?format=pdf — same renderCoverLetterPdf helper renders + returns application/pdf bytes via Content-Disposition: attachment. CoverLetterPanel.handleDownloadPDF refactored: fetch bytes → URL.createObjectURL → synthetic anchor click → revoke. ~50 lines of client-side jsPDF removed; the jspdf import dropped from CoverLetterPanel.tsx (jspdf still in package for TailoredCvCard's html2canvas-pro path). Preview-only overrides (hiring_manager_override / edited_body) still work — query params forwarded, server applies them inside assembleLetter before rendering. Legacy format-omitted JSON response kept for backward compat.

### Entry 59

Phase H-2 — Bulk pool actions (2026-05-22, commit 2a68616) — Scales the pool workflow for high-volume automation. When a big scrape produces 5-10 letters at once, deciding each individually is friction proportional to volume. NEW server actions in actions.ts: bulkMarkPoolNoEmail(jobIds[]) stamps pool_decision_at on still-undecided rows (.is('pool_decision_at', null) guard) — routes selected cards to 'Ready to apply'; bulkArchiveJobs(jobIds[]) stamps dismissed_at on rows not already dismissed/applied. Both return {updated} count; RLS scopes everything to the owning user. NEW components/applications/PoolBulkBar.tsx wraps the pool-tab card list — selection checkbox overlay in each card's top-left corner, 'Select all (N)' / 'Deselect all' toggle above the list, sticky bottom action bar with No email / Archive / dismiss buttons that appears when ≥1 selected. Other tabs (email/apply/sent/archived) still render plain ApplicationCards. Intentionally OMITTED 'Same email for all' — too risky if companies differ; the most damaging possible bulk mistake. Error surfaced inline in the bulk bar.

### Entry 60

Phase F — Gmail / Outlook OAuth email dispatch (2026-05-22, commits a3c65b0 + a4b1e2a + 8b93843 + 8ea5052) — Completes the automation loop end-to-end: scrape → analyze → letter → pool decision → in-app Send → marked applied. NEW Migration 034: email_integrations.oauth_token bytea→text (so AES-256-GCM encrypted base64 strings fit cleanly); cover_letters.email_sent_at timestamptz + email_sent_to text. NEW Migration 035 (BUG FIX): email_integrations.provider CHECK realigned from ('gmail','outlook') in 031 to ('google','microsoft') matching code — this was a SILENT FAILURE: callbacks ran successfully and redirected with email_connected=success but Postgres rejected every upsert via check_violation; table stayed empty and Connect button never flipped. NEW web/src/lib/email/{tokens.ts,gmail.ts,outlook.ts}: saveTokens/loadTokens/deleteTokens/getValidAccessToken — encrypts StoredTokens JSON via existing encryptApiKey, upserts service-role admin client, auto-refreshes within 5 min of expiry (Google non-rotating refresh tokens; Microsoft rotating — new refresh_token on every refresh). sendViaGmail builds RFC-2822 multipart/mixed with optional PDF attachment, base64url-encodes the raw email, POSTs to users.messages.send. sendViaOutlook builds Graph API sendMail message JSON with fileAttachment (@odata.type=#microsoft.graph.fileAttachment). NEW API ROUTES: GET /api/auth/email/{google,outlook} (CSRF state cookie + redirect to OAuth consent); GET /{google,outlook}/callback (state verify, exchange code, fetch user email via userinfo/Graph, saveTokens, redirect with ?email_connected=<provider> or ?email_error=<reason>); POST /api/auth/email/disconnect (deleteTokens); POST /api/applications/[letter_id]/send-email (fetch letter+job+latest analysis_run → download tailored PDF from tailored-cvs bucket → getValidAccessToken → sendViaGmail or sendViaOutlook with To: '<hiring_manager> <contact_email>' + Subject: 'Application for <title> at <company>' + Body: pass_3_final + Attachment: TailoredCV_<company>.pdf → stamp cover_letters.email_sent_at + jobs.applied_at). NEW components/email/EmailIntegrationCard with provider-branded Connect buttons (only renders when GOOGLE_CLIENT_ID/MICROSOFT_CLIENT_ID configured); EmailIntegrationCard mounted on /dashboard/integrations between AI providers and Job sources. NEW Send email button in ApplicationCard (email tab only, when contact_email + not applied — shows loader, inline red banner on error, slides card to Sent tab on success). NEW success/error banner on /dashboard/integrations reading ?email_connected / ?email_error query params — five known error keys mapped to friendly messages + special save_failed:<reason> handler. BUG-FIX PASS: saveTokens was silently swallowing Supabase upsert errors → added error check + throw; OAuth callbacks had no try/catch around saveTokens → wrapped, propagate error via ?email_error=save_failed:<reason>. Vercel logs confirmed via diagnostic stream that callback returned 200 but table stayed empty → led directly to the provider-name CHECK violation discovery. Required env vars (web): GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET (mandatory) + MICROSOFT_CLIENT_ID + MICROSOFT_CLIENT_SECRET (optional for Outlook) + NEXT_PUBLIC_APP_URL (REQUIRED — was empty in initial deploy, caused redirect_uri_mismatch; now set to https://jobtrackr-cv.vercel.app). Google OAuth setup: Cloud Console → Credentials → OAuth client (Web app) → authorized redirect URI = ${NEXT_PUBLIC_APP_URL}/api/auth/email/google/callback → enable Gmail API in Library → add test users on OAuth consent screen (until verification — sensitive scopes like gmail.send require Google verification for >100 users). Migrations + new env vars + redeploy required to take effect.

---

## Archived Planning Notes

### PHASE-10.2-READINESS

{
  "id": "PHASE-10.2-READINESS",
  "verdict": "PASS",
  "assessed": "2026-05-18",
  "reasoning": "Production voice_profiles fingerprint evaluated against Phase 2 acceptance criteria. Tells (5): all specific, actionable, grounded in the sample \u2014 directly usable as voice-transfer instructions in Phase 4. Formality (0.55): correctly calibrated, not collapsing to midpoint default. Vocabulary (moderate): matches actual sample register. Rhetorical devices (3): real structural patterns, usable for burstiness injection. Quantitative signals (avg sentence 18 words, stddev 6.4, paragraph openers, intensifiers): all usable. Phase 10.2 story library may proceed."
}

### PHASE-11-HANDOFF

{
  "id": "PHASE-11-HANDOFF",
  "status": "ready_for_planning",
  "logged": "2026-05-19",
  "title": "Phase 11 \u2014 Voice-directed drafting: multi-variant opening paragraphs",
  "problem": "Single-call generator gives the user one shot. If paragraph 1 misses tone or angle, the only recourse is Regenerate, which rewrites the whole letter and burns a model call. The voice fingerprint and company fact are both deterministic inputs \u2014 we can cheaply produce 3-4 structurally different openings on the first call and let the user pick.",
  "scope": "ONLY the opening paragraph (P1). Paragraphs 2-4 are generated from the chosen P1 in a second call so they cohere with the selected opener. Out of scope: full-letter variants (too expensive, too much UI), per-paragraph regeneration UI (defer to a later phase).",
  "open_design_questions": [
    "Should the 3-4 variants come from one model call (JSON array response) or four parallel calls (more entropy, 4x cost)? Recommend single call + JSON array \u2014 voice fingerprint pushes diversity better than independent calls of the same prompt.",
    "How do we present variants? Stacked cards with a 'Use this opener' button each? Or a tabbed view? Stacked is more honest about the comparison.",
    "Do we save the rejected variants for telemetry / future fine-tuning? Probably yes \u2014 new JSONB column `discarded_openings` on cover_letters.",
    "What happens on Regenerate after the user has picked? Re-pick from same 4, or generate fresh? Recommend fresh \u2014 picking should feel intentional, not constrained."
  ],
  "files_to_read_first": [
    "cv-backend/app/services/ai/prompts/cover_letter/generate.py \u2014 current P1 spec",
    "cv-backend/app/services/cover_letter/generator.py \u2014 current single-call flow",
    "web/src/components/cv/CoverLetterPanel.tsx \u2014 UI integration point",
    "docs/cover-letter-spec.md \u2014 full 10-part spec for context"
  ],
  "starting_prompt_for_next_session": "Read .claude/graph.json then PHASE-11-HANDOFF planning_note. Plan Phase 11 (voice-directed drafting multi-variant opening paragraphs) using the /plan slash command \u2014 produce the 10-section plan, then stop for my review."
}

### PHASE-A-AUTOMATION-GATES-READY-TO-APPLY

{
  "id": "PHASE-A-AUTOMATION-GATES-READY-TO-APPLY",
  "status": "applied_and_verified_2026-05-21",
  "logged": "2026-05-21",
  "title": "Phase A \u2014 Automation gates schema (Migration 031) written, awaiting Supabase apply + verification",
  "context": "Phase A of the larger gated-pipeline overhaul (see PIPELINE-AUTOMATION-DESIGN below). Pure schema, no code. Lays the foundation for: cheap pre-checks (jd_quality, role_match) that avoid AI calls; two-gate ATS model (skip tailoring below initial threshold, skip cover letter below final threshold); per-profile automation config; an applications outbox distinct from cover_letters; and a stub for email integration (Phase F).",
  "files_written": [
    "supabase/migrations/031_automation_gates_and_applications.sql \u2014 schema",
    "supabase/migrations/032_backfill_jd_quality_and_role_match.sql \u2014 backfill"
  ],
  "schema_summary": {
    "jobs": "added jd_quality ('rich'|'thin'|'unknown'), role_match ('match'|'mismatch'|'uncertain'), has_email (GENERATED ALWAYS AS contact_email IS NOT NULL)",
    "analysis_runs": "added initial_ats_score, passed_initial_gate, passed_final_gate, automation (default false)",
    "search_profiles": "added automation_enabled (false), min_initial_ats (55), min_final_ats (75), role_match_strict (false), auto_send_emails ('never'), daily_application_limit (10)",
    "cover_letters": "added auto_selected_variant_id",
    "applications (NEW)": "the outbox \u2014 channel/email_draft/email_subject/status/sent_at/sent_to/user_verified \u2014 RLS + Realtime + 2 indexes",
    "email_integrations (NEW)": "schema stub \u2014 provider/oauth_token/from_address \u2014 RLS only, no code"
  },
  "design_decision_pipeline_state": "DROPPED the pipeline_state + pipeline_blocked_reason columns from the original plan. State is DERIVED in JS (same pattern as components/jobs/progressFlags.deriveProgress) \u2014 single source of truth, no trigger maintenance burden, no consistency risk.",
  "user_action_required": [
    "Open Supabase SQL editor",
    "Run 031_automation_gates_and_applications.sql in a fresh query \u2014 verify no errors",
    "Run 032_backfill_jd_quality_and_role_match.sql in a fresh query \u2014 verify no errors",
    "Run the 5 verification queries from the PHASE-A plan (column distribution, RLS, policies, Realtime publication, search_profiles defaults)",
    "Report the verification output back \u2014 especially the jd_quality/role_match distributions, which will tell us if the heuristics are well-calibrated for the existing data"
  ],
  "after_verification": "Commit both .sql files to the repo. Add a `completed` entry to build_state. Update _meta.updated. Proceed to Phase B (visibility-only UI changes).",
  "phases_after_A": {
    "B": "Jobs table: State + JD columns, new chips (Needs JD / Role mismatch / Has email / Auto-skipped), dashboard banner for triage queues. Visibility only \u2014 no pipeline changes. NEW file: components/jobs/pipelineState.ts (pure deriver).",
    "C": "Two gate checks wired into the existing MANUAL pipeline. automation_enabled stays false everywhere \u2014 defaults make this invisible until a user opts in.",
    "D": "Applications tab on dashboard with 4 sub-tabs (Ready to email / Ready to apply / Sent / Archived). Apply-link channel works; email send is mocked.",
    "E": "Automation worker \u2014 scheduled job that runs the pipeline gate-by-gate for automation_enabled profiles, respecting daily limits.",
    "F": "Email integration \u2014 Gmail OAuth (confirmed Q1), send pipeline, auto-send modes."
  }
}

---

## Resolved Open Questions

- **Q1**: Provider support v1 — both Anthropic and OpenAI, or just Anthropic? (revisit_in: Phase 4) — RESOLVED
- **Q2**: Tailored PDF viewer — inline iframe or download-only? (revisit_in: Phase 7) — RESOLVED
- **Q3**: JD-scrape fallback UX — fail-and-retry or modal-paste? (revisit_in: Phase 8) — RESOLVED
