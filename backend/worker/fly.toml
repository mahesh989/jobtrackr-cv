app = 'jobtrackr-worker'
primary_region = 'syd'

[build]
  dockerfile = "backend/worker/Dockerfile"

[processes]
  worker = 'node dist/index.js'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'
  processes = ['worker']