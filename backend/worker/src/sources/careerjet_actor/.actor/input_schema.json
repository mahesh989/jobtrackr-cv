{
  "title": "Careerjet JD Fetcher Input",
  "description": "Input schema for careerjet-jd-fetcher.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "Job URLs",
      "type": "array",
      "description": "careerjet.com.au job URLs (post-filter survivors) to fetch full descriptions for.",
      "editor": "stringList",
      "example": ["https://www.careerjet.com.au/jobad/au4f774a0504ed1a561daa9486708f3005"]
    },
    "maxUrls": {
      "title": "Max URLs",
      "type": "integer",
      "description": "Hard cap on URLs fetched per run.",
      "editor": "number",
      "default": 25
    }
  },
  "required": ["urls"]
}
