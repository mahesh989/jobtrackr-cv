# frontend/web

The Next.js 16 (App Router) frontend for JobTrackr-CV.

For architecture, directory structure, conventions, and quick commands, see:
- [`/CLAUDE.md`](../../CLAUDE.md) — Claude/AI-agent instructions, doubles as the canonical dev reference
- [`/AGENTS.md`](../../AGENTS.md) — directory structure + patterns
- [`/README.md`](../../README.md) — project overview

Quick start:

```bash
npm run dev        # localhost:3000
npm run build       # production build
npm run lint        # eslint
npx tsc --noEmit    # type check
```
