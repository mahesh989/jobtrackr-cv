{
  "_meta": {
    "version": "0.17",
    "updated": "2026-06-25 (global bucket: single-flight locks + skip-when-fresh + admin observability shipped + per-vertical backend isolation merged to main)",
    "purpose": "Authoritative live state for JobTrackr-CV. Read at session start. Update on significant changes.",
    "changelog": "docs/build_log.md"
  },
  "project": {
    "name": "JobTrackr-CV",
    "summary": "AI-assisted job discovery + CV tailoring SaaS. One codebase, one Supabase DB, three services: frontend/web (Vercel), backend/worker (Fly), backend/api (Fly).",
    "repo": "https://github.com/mahesh989/jobtrackr-cv",
    "local": "/Users/mahesh/Documents/Github/jobtrackr-cv"
  },
  "services": {
    "web": {
      "path": "frontend/web/",
      "host": "Vercel (jobtrackr-cv)",
      "stack": "Next.js 16 (Turbopack) App Router, Tailwind v4, Supabase browser client",
      "deploy": "git push origin main \u2014 auto-deploys to production"
    },
    "worker": {
      "path": "backend/worker/",
      "host": "Fly.io (jobtrackr-worker)",
      "stack": "Node.js 22, TypeScript, BullMQ, curl_cffi subprocess",
      "deploy": "flyctl deploy --config /Users/mahesh/Documents/Github/jobtrackr-cv/backend/worker/fly.toml",
      "notes": "Stage-4b filter is title-only + optional teaser rescue (driven by profile.must_include_phrases, migration 046). Primary SEEK: seekDirect (curl_cffi Chrome-124 TLS, no proxy); Apify actor: fallback only via profile.seek_method='actor'. Careerjet: direct HTML scraping via curl_cffi (API abandoned to avoid tracking URL IP-blocks). Adzuna: API teasers by default (profile.adzuna_method='api'); opt-in 'direct' mode scrapes /details/<id> HTML for full JDs (migration 047, cap 50). Full-JD threshold: 1400 chars. Jora: disabled (OOM on 512MB VM)."
    },
    "cv_backend": {
      "path": "backend/api/",
      "host": "Fly.io (jobtrackr-cv-api)",
      "stack": "FastAPI, Python 3.11, ReportLab, Supabase service-role client",
      "deploy": "flyctl deploy --config /Users/mahesh/Documents/Github/jobtrackr-cv/backend/api/fly.toml",
      "status": "active"
    }
  },
  "build_state": {
    "current_phase": "phase-13 \u2014 monetization: Stripe subscriptions + usage metering (COMPLETE, LIVE in production as of 2026-06-24)",
    "billing_high_level": "Adding paid subscriptions (Stripe, AUD) gating the platform. AI cost is now the platform's (BYOK removed 2026-06-16, see D20) \u2014 plan caps/pricing below were set when AI cost sat with the user and need revisiting. Plans: 3-day card-upfront TRIAL (3 tailored CVs + 3 cover letters, 1 profile, 1 run) \u2192 WEEKLY (50 unique / 75 total CVs + letters, 5 profiles, 30 runs) \u2192 MONTHLY (250 unique / 375 total, 10 profiles, 120 runs) \u2192 UNLIMITED (\u221e, higher price). Total = unique \u00d7 1.5. Tailored CV and cover letter are SEPARATE buckets, equal caps (either/or). Caps reset per Stripe billing period (counted by current_period_start). Auto-renew ON (Stripe default; trial auto-converts after 3 days; subscriptions auto-renew each period). On cancel/expiry \u2192 READ-ONLY (existing CVs/letters viewable, no new analyses). Existing role='beta' users grandfathered via a comp subscription with a 14-day grace. Enforcement is at the 4 web-layer choke points only (createProfile, profiles/[id]/run, jobs/[id]/analyze, jobs/[id]/cover-letter); cv-backend + worker untouched. Migration 051_billing.sql. Pricing numbers are placeholders to finalize before go-live.",
    "previous_phase": "post-12 \u2014 CV tailoring quality + polish",
    "production_writer": "w8_verified (W3/W8+) \u2014 now the CODE DEFAULT in config.py (TAILORED_CV_WRITER: str = 'w8_verified'). Also set as a Fly secret on jobtrackr-cv-api for belt-and-braces. Legacy W1 still exists inside W3 as a building block (run_tailored_cv called by _impl.py); it is NOT dead code. Revert via TAILORED_CV_WRITER=legacy Fly secret.",
    "production_ats_scorer": "PRODUCTION (main): S1 v1 deterministic 50/35/15. REFACTOR BRANCH (refactor/architecture-review): ATS v2 = 50 keyword / 40 experience / 10 formatting. Experience score REBUILT from three CV<->JD-alignment sub-signals: responsibility coverage (20, JD duties <-> CV bullets), relevant tenure (12, CV months in JD vertical vs experience_years_required), vertical alignment (8, %% CV entries in JD vertical). Removed v1's two defects: required-keyword double-count + role-family freebie. Tailoring invariant: keyword injection moves Cat 1 only (experience reads the narrative, formatting reads structure - neither fabricable from the feasibility plan, so predicted lift == actual lift). New module app/services/cv/experience_parser.py. min_initial_ats lowered 60->50 to match honest scoring. Full design: backend/api/docs/ATS_SCORING_V2.md.",
    "last_shipped": "2026-06-24 (Cloudflare Turnstile bot protection \u2014 CODE SHIPPED + USER-ACTIVATED + CONFIRMED WORKING IN PRODUCTION, commit 1a9d8aa on main, deployed to Vercel) \u2014 Turnstile-only (NO DNS proxy/WAF \u2014 user briefly tried Cloudflare's 'Connect your domain' DNS-proxy flow by mistake, got 'Invalid domain' on the vercel.app hostname; correct path is dash.cloudflare.com \u2192 Application security \u2192 Turnstile \u2192 Add widget, NOT the Websites/DNS flow). TWO layers: (L1) Supabase-native CAPTCHA on the live client-side login + signup flows \u2014 shared TurnstileBox widget (@marsidev/react-turnstile) in app/auth/login+signup/page.tsx passes captchaToken to signInWithPassword/signUp; widget resets per attempt (single-use tokens); submit gated only when NEXT_PUBLIC_TURNSTILE_SITE_KEY set; Google OAuth exempt. (L2) fail-closed verifyTurnstile() helper (frontend/web/src/lib/turnstile.ts) wired into the DORMANT invite routes api/auth/signup + validate-invite (no-op until TURNSTILE_SECRET_KEY set). KEY CONSTRAINT (still true going forward): jobtrackr-cv + old JobTrackr share Supabase project ltcqqlfsomqxuwfcxxbe, and Supabase CAPTCHA is a PROJECT-GLOBAL toggle \u2014 was safe to enable ONLY because old JobTrackr is RETIRED (would otherwise break its email/pw logins; revisit if it's ever revived). ACTIVATION DONE by user: Cloudflare Turnstile widget created (Application security \u2192 Turnstile, Managed mode, hostname jobtrackr-cv.vercel.app/jobtrackr.app), site+secret keys set in Vercel env + redeployed, Supabase Auth \u2192 Bot & Abuse Protection \u2192 CAPTCHA \u2192 Turnstile enabled with the secret. User confirmed login/signup work correctly with the widget live. Rollback if ever needed = toggle Supabase CAPTCHA off. Plan file: ~/.claude/plans/moonlit-weaving-crescent.md. PREVIOUS: 2026-06-23 (tier-gated sources + tracking, commit 86dfca3) \u2014 THREE changes on main. (1) SEEK Apify listings fallback gated to unlimited tier only; weekly/monthly use SEEK direct only (no paid fallback). (2) SEEK JD Apify fallback removed entirely \u2014 SEEK JD enrichment is free-direct-only for all tiers. Two paid Apify actors total, both unlimited-only: SEEK listings actor + Adzuna JD-fetcher actor. Adzuna actor failure now logs clearly (falling back to API teasers). (3) SOURCE METHOD TRACKING: sourceMethods object built per run (tier, per-source method/enrichment/fallback/counts), persisted to run_logs.source_methods (migration 065 \u2014 additive ALTER TABLE). PlatformSourcesCard rewritten as read-only 3-column matrix (Weekly/Monthly/Unlimited); /api/admin/sources route deleted. Admin Sourcing health page: source method health KPIs (SEEK failures, Apify fallbacks, Adzuna actor failures) + Tier + Source methods columns in Recent runs table. PENDING (manual, in order): apply 062\u2192063\u2192064\u2192065 migrations in Supabase SQL editor; flyctl deploy --config backend/worker/fly.toml. PREVIOUS: 2026-06-23 (admin-sources + view-as session, commit f4512de) \u2014 THREE changes on main. (1) JOB SOURCES \u2192 ADMIN GLOBAL: migration 063 platform_sources (single row, RLS service-role only) holds enabled_sources + adzuna_method + seek_method; admin Integrations page got a PlatformSourcesCard; GET/PATCH /api/admin/sources; orchestrator.loadPlatformSources() OVERRIDES profile.enabled_sources/adzuna_method/seek_method at run time (per-profile columns now vestigial). ProfileForm: entire Sources section deleted (SourceRow/RadioRow/SourceId removed); Schedule renumbered 4, Automation 5. extractSourceFields() in actions/_helpers.ts still writes defaults to vestigial cols \u2014 harmless, orchestrator ignores them. (2) EMAIL REMOVED from admin Integrations (users still connect Gmail/Outlook at My CV \u2192 Email account). Integrations is now Job sources + Apify quota only. (3) ADMIN 'VIEW AS USER': /api/admin/view-as?mode=user|admin sets/clears jt_user_view cookie. layout reads cookie \u2192 renders user nav + fetches user sidebar data; dashboard page skips admin\u2192/dashboard/admin redirect in user-view. SidebarNav: 'View as user' link in admin nav (Preview section); 'Back to admin' banner in user-view. Always the admin's OWN account \u2014 not impersonation. PENDING (manual, in order): apply migration 062 (jd_quality 1000) THEN migration 063 (platform_sources) in Supabase SQL editor; flyctl deploy --config backend/worker/fly.toml (covers JD_MIN_USABLE=1000 + fullRefresh + platform-token fallback + loadPlatformSources). PREVIOUS: 2026-06-23 (sources full-JD + JD-threshold session) \u2014 Careerjet/Adzuna full-JD investigation + Apify-actor enrichment, on main. (1) CAREERJET: direct HTML scrape is Cloudflare-Turnstile-blocked from every cloud path (verified: datacenter Turnstile-blocks even a real browser; Apify residential gets a 'Verification required' interstitial; even residential+Playwright+warm-up tarpitted). Reverted careerjet to the FREE v4 API (search.api.careerjet.net, IP-whitelisted) for listings (snippet ~251 chars); built a careerjet-jd-fetcher actor (now dormant, gated on CAREERJET_ACTOR_ID); re-ranked careerjet SOURCE_BONUS 1800->300 (below adzuna 400) since it only carries a snippet (winner.ts). (2) ADZUNA full-JD WORKS: /details/<id> 429-rate-limits the Fly IP (Retry-After 3600) but a residential IP returns clean static HTML (section.adp-body). Built adzuna-jd-fetcher actor (CheerioCrawler + Apify RESIDENTIAL AU, no browser); listings stay free via the Adzuna API; stage 7d enriches survivors via enrichAdzunaJDsViaActor when ADZUNA_ACTOR_ID + Apify token present. Verified in prod: '4/4 full descriptions merged'. (3) PLATFORM APIFY TOKEN: loadApifyIntegration() falls back to any founder/admin Apify integration when the profile owner has none (spend tracked on admin row). (4) FULL REFRESH run option (queue->worker->orchestrator, no migration) re-runs the deep 28-day window on demand; RunNowButton shows a 'Full refresh' button beside 'Run now'. (5) PIPELINE CONSOLE persists after a run (LiveLogConsole tracks latest run regardless of status; runs route orders desc). (6) JD THRESHOLD aligned to 1000: jd_quality classifier (migration 062, replaces 050's 1400), analyze JD_FULL_THRESHOLD, worker JD_MIN_USABLE, jobs.ts manual-add \u2014 all 1400->1000 to match MANUAL_JD_MIN_CHARS. Apify actors: prospect_fuzz~adzuna-jd-fetcher (live), ~careerjet-jd-fetcher (dormant). PENDING MANUAL: apply migration 062 in Supabase SQL editor; flyctl deploy worker for JD_MIN_USABLE + fullRefresh + platform-token. PREVIOUS: 2026-06-22 (Aurora UI + summary/credential reliability session) \u2014 TWO tracks, all on main. TRACK A \u2014 AURORA SIGNATURE THEME (frontend, scoped, zero regression to other 5 themes): new dual theme aurora-dark (DEFAULT) + aurora-light (teal+violet accent; Space Grotesk display / Plus Jakarta Sans body / JetBrains Mono numerals; WCAG-AA token pairs), density/text-size control (lib/density.ts), donut tokenised to aurora palette (solid per-slice so legend matches), SOLID teal buttons (gradient reverted per user), custom gradient checkboxes, native <select> color-scheme dark (kills the bright-blue popup), and a broad remap of hardcoded Tailwind light-palette utilities (bg/border/text -50/100/200/600/700/800 across green/amber/red/blue/teal/violet) so every chip/card/callout reads on dark \u2014 covering JobEditModal, FeasibilityCard, AnalysisRunClient skill chips, SmartFeed source pills, Voice/Stories, EmailIntegrationCard, FunnelChip, dashboard callouts. See OPS-26..29; docs/aurora-preview.html is a standalone reference. TRACK B \u2014 TAILORED-CV / JD RELIABILITY (backend; stamp-then-strip + extraction bugs): (OPS-30) _enforce_company_anchor repairs the PARTIAL cherry-pick (one employer named, other dropped) by splicing the missing employer + present-perfect->past tense; (OPS-31) availability line was stamped then deleted by verify_claims \u2014 fixed via verify._collect_summary skip + idempotent re-stamp after verify; (OPS-32) the employer anchor itself was stamped pre-verify then stripped by verify_claims \u2014 fixed by re-running _enforce_company_anchor in the post-verify chain of _writer_w8_verified; (OPS-33) credential trim drops trailing prose adjectives + dangling connectors ('Bachelor of Nursing and ready' -> 'Bachelor of Nursing'). Availability also MOVED from Registration & Licences to the END of the Professional Summary (stamp_availability_in_summary). 1167 backend tests pass; tsc + next build clean. PENDING: one more cv-backend deploy (flyctl deploy -a jobtrackr-cv-api) to make OPS-30/31/32/33 fully live. PREVIOUS: 2026-06-21 (My CV session) \u2014 \"My CV\" unified page + build-a-CV-from-scratch feature (commits dd0e74b, 151532a, 7192cf3, f3b6b70, 643d7de, bebab40 on main). (1) Merged My Details + CV Library into one /dashboard/cv page (ProfileDetailsClient: single React-context state + one PATCH to /api/user/preferences to avoid wholesale-replace clobber; old /dashboard/settings/profile redirects here preserving OAuth params; SidebarNav collapsed to one \"My CV\" item). (2) Build-from-scratch: POST /api/cv/create makes a blank cv_versions row with a built:// sentinel in pdf_storage_path (no migration); CvReviewClient gained mode=\"create\"; built CVs flow through the SAME analysis pipeline via normalized_cv_text (structured PATCH also syncs cv_text for built CVs); ensureActive/DELETE/account-delete guard the built:// sentinel. (3) Vertical-aware skill labels (lib/cv/skillLabels.ts, driven by role_families). (4) AI skill extraction for built CVs: POST /api/cv/[id]/extract-skills -> /internal/categorise-cv, \"Suggest from experience\" button merges into buckets. (5) Portfolio projects moved from profile to per-CV: StructuredCvProject + ## Projects/custom_sections now rendered by cv_renderer.py (1162 backend tests pass); analyze + resume routes drop the old contact_details.projects merge (prevents double ## Projects). (6) Builder UX: theme-aware bordered inputs, fixed bullet alignment, custom sections, mandatory-field validation with red borders (builder: Experience+Education; profile: Name/Phone/Email/Suburb/State/Postcode + >=1 vertical), two-button Save-as-draft / Save, no autosave in create mode. (7) Misc: forest-green \"Reviewed\" chip, EmailIntegrationCard text uses theme vars, per-group \"Select all / Deselect all\" in job board select mode, search-profile save now redirects to /dashboard/profiles (Job Searches) instead of /dashboard. TypeScript + eslint clean on all changed files. PREVIOUS: 2026-06-21 \u2014 availability feature + summary/credential/pipeline fixes (all on main). (1) OPT-IN AVAILABILITY chip (PR #7 + follow-ups): profile credentials gained availability[] + show_availability; renders as its OWN italic line under ## Registration & Licences (build_availability_line + stamp_credentials, contact_line.py), off by default, role-gated nursing/manual, JSON-only no migration. See OPS-22. (2) MULTI-ROLE SUMMARY ANCHOR (OPS-23): _ensure_summary_anchors_both_employers generation-time retry in _impl.py _writer_w8_integrated \u2014 when CV has 2+ multi-month employers but summary S2 names <2, re-asks model to rewrite S2 into two semicolon clauses naming both; deterministic _enforce_company_anchor 'any()' guard couldn't repair a cherry-picked/award-shaped S2. (3) CREDENTIAL TRIM (OPS-24): _trim_qual_phrase now stops at orphan ')'/sentence period (_CRED_TAIL_TERMINATOR_RE), fixing 'Cert III in Disability or Individual Support). Strong' prose-leak; comma still allowed. (4) WORKER KEYWORD RESCUE (OPS-25): applyKeywordFilter teaser-rescue was dead because it keyed on url_hash (empty until dedup stage) \u2014 switched to job.url. CONFIRMED root cause of 'no new jobs since June 8' on profile AIN Sydney (stage 4b dropped 63/76 genuine AIN jobs). (5) DESC-EXCLUSION DIAGNOSTICS: per-phrase breakdown logged at stage 4c/7b (formatExcludeBreakdown) + 100%-drop tripwire WARNs at 4b/4c/7b. Backend 1162 tests green; worker verified via tsx repros (no worker test harness). PENDING DEPLOYS: cv-backend (flyctl deploy --config backend/api/fly.toml) for credential-trim fix; worker (flyctl deploy --config backend/worker/fly.toml) for keyword-rescue + diagnostics. PREVIOUS: 2026-06-20 \u2014 JD-analysis pipeline overhaul + tailoring/credential fixes (PR #4, merged to main). Fixes: phantom leadership (weak-grounding token guard), cert-IV satisfies cert-III (AQF subsumption ladder scoped to care family), credential prose-capture eliminated (_trim_qual_phrase stops at parenthetical, component labels routed to noise), award duplication with middle-dot separator fixed, employer extraction handles inline-date headings, anchor injection broadened dangling-clause guard, AHPRA word-boundary fix, CI fix for _apply_setting_bridge re-export. Also merged PR #5 (eslint safe cleanup: prefer-const, dead directives, unused imports). 1111 backend tests passing, TypeScript clean. Phase 13 billing: code-complete (migration 051\u2013061, entitlements, Stripe routes, 4 choke points, billing dashboard, onboarding/plan page) \u2014 awaiting Stripe env vars in Vercel to go live. PREVIOUS: 2026-06-19 \u2014 professional summary quality fixes (7 commits, all on main, deployed to jobtrackr-cv-api). Root cause: W3 was defaulting to W1 in production (TAILORED_CV_WRITER env not set + code default='legacy'). Fixes: (1) 7c65aa0 \u2014 flip code default to w8_verified so W3 is active without relying on a Fly secret. (2) 94920be \u2014 TENURE TIEBREAKER in composition.py: adds MULTI-ROLE framing tier so candidates with 2+ multi-month named roles are never hidden behind 'across multiple settings' breadth framing. (3) af0bb6c \u2014 _lowercase_generic_care_phrases enforcer: forces 'activities of daily living', 'dementia care', 'electronic medication administration' etc. to lowercase mid-sentence (LLM was Title-Casing them). (4) dc92213 \u2014 TITLE SLOT hard example in composition.py: forces opener to use JD job title exactly, not a CV synonym (was writing 'Personal Care Worker' when JD said 'Assistant in Nursing'). (5) c66cbc3 \u2014 _enforce_company_anchor: deterministic enforcer that appends 'at Employer1 and Employer2' to S2 when the LLM omits employer names and the CV has 2+ multi-month roles. (6) d3685f9 \u2014 guard in _enforce_company_anchor: skips injection when S2 ends with a dangling preposition/conjunction (avoids making an already-broken sentence worse). (7) 1c4be25 \u2014 _title_case_role helper: keeps connector words lowercase in the role title ('Assistant in Nursing' not 'Assistant In Nursing'). Also: Fly secret TAILORED_CV_WRITER set to w8_verified on jobtrackr-cv-api. 17 backend tests green. EARLIER 2026-06-14 \u2014 structured CV review form v2 (PR #2). 2026-06-10 \u2014 JD-analysis quality sweep + summary honesty gates. 2026-06-05 Phase 3B lexicon foundation. 2026-06-02 post-verify skills hygiene + Awards bullet format. 2026-05-30 nursing sub-type labels + cover-letter hardening + adaptive PDF layout.",
    "next_session": "PHASE 13 COMPLETE AND LIVE (2026-06-24). All billing code merged to main. Migrations 062-065 applied in Supabase. Worker deployed (tier-gated sources live). Stripe env vars set in Vercel. Billing is LIVE in production.\n\nThis session also added:\n  - Two-step upgrade confirmation in UpgradeOptions (arm then confirm, amber price warning)\n  - OpenAPI spec for all 52 routes at .postman/collections/jobtrackr-api.openapi.yaml\n  - logo.png added to frontend/web/public/; used in SidebarNav, landing page, login, signup\n  - platform_source_tiers table (migration 064) wired to orchestrator (tier-gated Apify actors)\n\nNEXT: Phase 14 \u2014 user acquisition / public launch prep. Suggested first steps:\n  1. End-to-end test the full subscription flow as a real new user (trial \u2192 subscribe \u2192 upgrade)\n  2. Set up Stripe webhook monitoring in the Stripe dashboard\n  3. Consider adding email receipts/confirmations (Resend) for subscription events\n  4. Review pricing \u2014 Weekly A$9.99/wk has NO caps (unlimited), same as Unlimited A$29.99/mo. Monthly A$19.99/mo has caps (250 CVs/letters, 10 profiles, 120 runs). Decide if that hierarchy makes sense commercially.",
    "completed_summary": "All core phases 0\u201312 complete: infra, DB migrations (001\u2013048), CV upload, BYOK AI keys, full 9-step analysis pipeline, tailored CV PDF (w8_verified production writer), cover letter generation, Gmail/Outlook OAuth dispatch, applications outbox, voice-directed drafting, analytics funnel, pipeline automation (gate-based auto-tailoring), worker sources (SEEK/Adzuna/Careerjet/Greenhouse/Lever), PipelineDonut (5 lenses), SmartFeed/SmartToolbar, 5-theme system, per-profile source selection, client-side instant filtering on both boards, driving-distance feature.",
    "active_branch_work": "branch refactor/architecture-review (NOT merged to main; user merges later). Carries: (1) JD-extraction quality programme Phases 1-4 (evidence-grounded extraction + groundedness gate; multi-bucket recall floor; subsumption dedup; golden-JD regression harness). (2) ATS scoring v2 (50/40/10 envelope, experience score rebuilt from CV<->JD alignment). (3) Lexicon fixes from real Jesmond AIN re-runs (night-shift/partnership noise, partnership-building + recreational-activities canonicals). 915 tests passing. See backend/api/docs/PHASE_2_PLUS_STATUS.md and backend/api/docs/ATS_SCORING_V2.md. || branch feat/vertical-pipeline (NOT merged to main; user merges later). Per-vertical backend isolation — all 5 phases complete (A: verticals package + registry; B: per-vertical JD-analysis prompt hints; C: consolidated family→lexicon map; D: nursing hooks self-contained in verticals/nursing/hooks.py; E: lexicons co-located in vertical folders). No pipeline logic forked. Backward-compat shim in role_families.py keeps all 7 existing importers unchanged. Nursing subtype overlay (Clinical/Care/Core Skills) verified passing."
  },
  "architecture": {
    "client_side_filtering": {
      "summary": "Both dashboard (/dashboard) and profile board (/dashboard/profiles/[id]/jobs) use instant client-side filtering.",
      "pattern": "Server fetches all \u2264200 non-dismissed jobs once, computes progress/pipelineState/atsBand. Client component holds full set and applies filterJobs+sortJobs in useMemo. Stage/triage/sort/keywords use shallowSetParams (History API) \u2014 no server round-trip. Location/posted_within/dismissed still hit server.",
      "key_files": [
        "frontend/web/src/components/jobs/JobBoard.tsx",
        "frontend/web/src/components/jobs/ProfileJobBoard.tsx",
        "frontend/web/src/components/jobs/jobFilters.ts",
        "frontend/web/src/components/jobs/shallowNav.ts"
      ]
    },
    "pipeline_automation": {
      "lifecycle": "DISCOVERED \u2192 JD_CHECK \u2192 ROLE_CHECK \u2192 INITIAL_ATS_GATE(60) \u2192 TAILORED_CV \u2192 FINAL_ATS_GATE(70) \u2192 COVER_LETTER+EMAIL \u2192 APPLICATION_READY \u2192 READY_TO_SEND/APPLY \u2192 SENT/APPLIED",
      "gates_save_cost": "JD_CHECK: zero AI cost; ROLE_CHECK: zero AI cost; INITIAL_GATE: saves ~3 AI calls; FINAL_GATE: saves ~2 AI calls",
      "cover_letter": "Co-generated with email body+subject in ONE AI call. Voice profile (fingerprint + sample) drives register."
    },
    "billing": {
      "summary": "Stripe subscriptions (AUD) monetize the platform. Enforcement lives ONLY in the web layer at 4 choke points; backend/api and backend/worker are unaware of billing.",
      "model": "Two metered buckets (tailored_cv, cover_letter) + profiles (live count of search_profiles) + runs (per-period event count). consume_usage() Postgres RPC does an atomic cap-check + reservation (FOR UPDATE on subscription). Reserve on trigger (pending) \u2192 trigger on analysis_runs/cover_letters status flips to committed (completed) or voided (failed). Dangling pending self-heals after 1h TTL in the count.",
      "entitlement_resolution": "founder/admin \u2192 unlimited bypass. status='trialing' \u2192 TRIAL limits (3/3/1/1) regardless of chosen plan, window = trial period. status='active' \u2192 chosen plan limits. status='past_due' \u2192 access continues (dunning banner) until Stripe moves to canceled/unpaid. plan='comp' (grandfathered beta) \u2192 unlimited until current_period_end (14-day grace) then read-only. canceled/unpaid/no-sub \u2192 READ-ONLY + redirect to /onboarding/plan.",
      "key_files": [
        "shared/supabase/migrations/051_billing.sql",
        "frontend/web/src/lib/billing/plans.ts",
        "frontend/web/src/lib/billing/entitlements.ts",
        "frontend/web/src/lib/billing/stripe.ts",
        "frontend/web/src/app/api/billing/checkout/route.ts",
        "frontend/web/src/app/api/billing/portal/route.ts",
        "frontend/web/src/app/api/billing/webhook/route.ts",
        "frontend/web/src/app/(dashboard)/dashboard/billing/page.tsx",
        "frontend/web/src/app/onboarding/plan/page.tsx"
      ],
      "env": [
        "STRIPE_SECRET_KEY",
        "STRIPE_WEBHOOK_SECRET",
        "NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY",
        "STRIPE_PRICE_WEEKLY",
        "STRIPE_PRICE_MONTHLY",
        "STRIPE_PRICE_UNLIMITED"
      ]
    },
    "structured_cv_review_form": {
      "summary": "Every uploaded CV passes through a normalising review form before the analysis pipeline consumes it. Removes input variance across thousands of CV layouts. Edits are rearrangement only \u2014 verbatim words preserved.",
      "version": "STRUCTURED_CV_VERSION = 4 (frontend/web/src/lib/cvBackend.ts + backend/api/app/services/cv/cv_structurizer.py \u2014 bump in lockstep). Review page silently re-runs structurize when stored _version is below this.",
      "data_flow": "Upload \u2192 /api/cv (POST) runs structurizeCv + categoriseCv in parallel \u2192 merges categoriser skills into structured_cv.skills (mirror) \u2192 renders normalized_cv_text via cv-backend /internal/render-canonical-cv \u2192 INSERT cv_versions row (cv_text, categorised_skills, structured_cv, structured_cv_status='parsed', normalized_cv_text, is_active iff first upload). Review form PATCH /api/cv/[id]/structured re-renders normalized_cv_text and keeps categorised_skills in lockstep with structured_cv.skills. /api/jobs/[id]/analyze reads normalized_cv_text when present, falls back to cv_text.",
      "schema_shape": "{ summary, experience[], education[], awards[], languages[], certifications[], skills{technical,soft_skills,domain_knowledge}, references[], gaps[], _version }. NO contact (profile is source of truth via stamp_contact_line).",
      "bucketing_rules": [
        "Care-sector VET quals (Cert III/IV / Diploma in Ageing Support, Individual Support, Community Services, etc.) live in education[] ONLY. Router moves any leaked-into-certifications copy and dedupes by fuzzy key that strips AU unit-code prefix (CHC43015 - X == X).",
        "Awards / Recognition / Honours / Commendations / Achievements / Scholarships \u2192 awards[] (not certifications). Recognized by section heading OR by award pattern (X Award, X Scholarship, Recognized for...) even when PDF mashes the heading into adjacent prose.",
        "Languages \u2192 own block (preserved but not used by tailored-CV composer).",
        "Experience sorted reverse-chronological by parsed end_date; Present/is_current ranks highest, undated last."
      ],
      "key_files": [
        "backend/api/app/services/ai/prompts/cv_structurization.py",
        "backend/api/app/services/cv/cv_structurizer.py",
        "backend/api/app/services/cv/cv_renderer.py",
        "backend/api/tests/test_cv_structurizer.py",
        "backend/api/tests/test_cv_renderer.py",
        "frontend/web/src/lib/cvBackend.ts",
        "frontend/web/src/lib/cv/structurizeAndCategorise.ts",
        "frontend/web/src/lib/cv/ensureActive.ts",
        "frontend/web/src/app/api/cv/route.ts",
        "frontend/web/src/app/api/cv/[id]/route.ts",
        "frontend/web/src/app/api/cv/[id]/structured/route.ts",
        "frontend/web/src/app/api/cv/[id]/structurize/route.ts",
        "frontend/web/src/components/cv/CvReviewClient.tsx",
        "frontend/web/src/components/cv/CvLibraryClient.tsx",
        "frontend/web/src/app/(dashboard)/dashboard/cv/page.tsx",
        "frontend/web/src/app/(dashboard)/dashboard/cv/[id]/review/page.tsx"
      ],
      "cv_library_ux": "CV cards expand inline (no route change); entire card is the click target with hover-lift; Expand/Collapse button. References card matches the same shell. Active CV is always first. Eager-loaded structured_cv makes expand instant; partial CVs still hit POST /structurize on demand. ensureSomeoneActive() runs on the page load AND on every mutate so the library never shows zero-active when CVs exist.",
      "application_pool_rule": "/dashboard/applications pool requires a COMPLETE set per job: cover letter (status=completed, !stale) + analysis_run (!stale) + tailored CV path (tailored_pdf_storage_path OR tailored_cv_storage_path). Sidebar badge count uses the same rule (frontend/web/src/app/(dashboard)/layout.tsx) so they cannot diverge. Sent tab unchanged (applied_at NOT NULL AND NOT dismissed).",
      "tailored_cv_references_format": "Plain-text lines, no markdown table. Format: '**Name**, Job title, Company \u2014 email@example.com'. Header row 'Referee | Email' and the markdown divider line were removed from build_references_block in backend/api/app/services/cv/contact_line.py."
    },
    "css_theming": {
      "rule": "Always use var(--brand), border-border, text-text, bg-surface. NEVER hardcode hex.",
      "default": "Notion is the project default (getStoredTheme() + the FOUC script in app/layout.tsx fall back to 'notion' when nothing is stored; an explicit 'default' choice is still honoured).",
      "themes": [
        "Default (#0969DA)",
        "Classic (#3B82F6)",
        "Gilded Noir (gold)",
        "Notion (purple #5645D4) \u2014 DEFAULT",
        "Clay (#FF4D8B)"
      ],
      "public_pages": "Landing (app/landing.css scoped to .landing), auth (login/signup inline styles) and privacy do NOT consume theme tokens \u2014 they were hand-restyled to the Notion palette (lavender canvas #E6E0F5 / surface #F4EEFB, purple #5645D4 accent, deep purple/navy #241A5C dark sections) + Manrope/Noto Serif. Changing the Notion theme later won't auto-update these."
    }
  },
  "bridge_contract": {
    "internal_analyze": {
      "method": "POST",
      "path": "/internal/analyze",
      "auth": "HMAC-SHA256(secret, X-Timestamp + body)",
      "body": [
        "run_id",
        "user_id",
        "jd_text",
        "jd_source_url",
        "jd_meta",
        "cv_version_id",
        "cv_text",
        "ai_provider",
        "ai_api_key",
        "ai_model (optional)"
      ],
      "response": "202 Accepted { run_id }"
    },
    "internal_extract_cv_text": {
      "method": "POST",
      "path": "/internal/extract-cv-text",
      "body": [
        "storage_path"
      ],
      "response": "{ cv_text }"
    },
    "internal_research_company": {
      "method": "POST",
      "path": "/internal/research-company",
      "auth": "HMAC-SHA256",
      "body": [
        "company_name",
        "company_domain (optional)",
        "ai_provider",
        "ai_api_key",
        "ai_model (optional)"
      ],
      "response": "{ company_id, status, research: CompanyResearch }"
    },
    "internal_scrape_jd": {
      "method": "POST",
      "path": "/internal/scrape-jd",
      "body": [
        "url"
      ],
      "response": "{ jd_text, job_title }"
    }
  },
  "decisions": [
    {
      "id": "D1",
      "decision": "Two services \u2014 cv-backend stays Python (FastAPI + ReportLab). Preserves 100% of pipeline logic."
    },
    {
      "id": "D2",
      "decision": "Shared Supabase project with production JobTrackr. FK from analysis_runs.job_id \u2192 jobs.id requires same DB."
    },
    {
      "id": "D3",
      "decision": "SUPERSEDED 2026-06-16 by D20. (Historical: BYOK Anthropic + OpenAI; no platform AI credits. Zero ongoing cost; user controls spend.)"
    },
    {
      "id": "D4",
      "decision": "HMAC-signed internal API between web and cv-backend. Simpler than JWKS; one caller."
    },
    {
      "id": "D5",
      "decision": "Realtime via Supabase postgres_changes on analysis_runs. Same pattern as cv-magic; no polling tax."
    },
    {
      "id": "D6",
      "decision": "Stale strategy: new run on (user, job) marks prior runs is_stale=true. Re-run is user-triggered only."
    },
    {
      "id": "D9",
      "decision": "SUPERSEDED 2026-06-16 by D20 \u2014 see below. (Historical: preferred AI provider was stored in localStorage, per-browser, no DB.)"
    },
    {
      "id": "D11",
      "decision": "Dropdowns inside overflow:hidden use createPortal + position:fixed + getBoundingClientRect."
    },
    {
      "id": "D14",
      "decision": "Cover letter: single rich AI call + verbatim voice sample + intent rubric + anti-patterns. Replaces 3-pass pipeline that produced mechanical output."
    },
    {
      "id": "D15",
      "decision": "Monetize the platform via Stripe subscriptions (AUD), NOT AI tokens \u2014 BYOK stays for now. Subscription bounds infra/Apify cost (job discovery, scraping, pipeline orchestration)."
    },
    {
      "id": "D16",
      "decision": "SUPERSEDED 2026-06-16 by D20. (Historical: BYOK was transitional \u2014 planned removal in the future, platform would supply AI keys. When removed, AI cost becomes the platform's and plan caps/pricing must be revisited \u2014 that revisit is now live, see D20.)"
    },
    {
      "id": "D17",
      "decision": "Prioritise direct scraping (seekDirect / Adzuna direct / Careerjet HTML) over the Apify actor to cut per-run cost \u2014 actor remains fallback only. Reinforces why runs are capped per plan."
    },
    {
      "id": "D18",
      "decision": "Billing enforcement is web-layer-only at 4 choke points (createProfile, run, analyze, cover-letter). cv-backend + worker stay billing-unaware. consume_usage() RPC + DB triggers are the authoritative meter (reserve/commit/void)."
    },
    {
      "id": "D19",
      "decision": "On subscription cancel/expiry \u2192 READ-ONLY (existing artifacts viewable, no new work) rather than hard-lock. Grandfather existing beta users with a 14-day comp subscription."
    },
    {
      "id": "D21",
      "decision": "Summary honesty is enforced at GENERATION time in the composer prompt, NOT by deterministic post-passes. 2026-06-16: deleted the dead summary-enforcement code that ran only in tests and never in production \u2014 app/services/eval/writers/summary.py (enforce_summary_concreteness + ~14 helpers), enforce_summary_years_gate + enforce_summary_word_floor + _find_summary_block in honesty_guard.py, the dead _strip_canned_summary_phrase/_CANNED_SUMMARY_RE in bridges.py, and their tests (test_sprint_e_summary.py, test_summary_honesty_fixes.py, two test classes in test_honesty_guard.py). Their intent (no inflated years claim, must name employer/scope/metric, 35-word floor) already lives in the production prompt's CAREER-STYLE SUMMARY block. assess_honesty_risk stays \u2014 it IS live (logged signal at _impl.py). Net \u2212~1,500 LOC; one source of truth for summary behaviour = the prompt."
    },
    {
      "id": "D20",
      "decision": "BYOK removed 2026-06-16. Single platform-wide AI provider/key/model, admin-controlled, replaces per-user user_integrations rows for anthropic/openai/deepseek. New table public.platform_ai_settings (one row per provider, partial unique index enforcing exactly one is_active=true). Every consumer route (analyze, resume, cover-letter, company-research, stories/extract, voice-profile, cv upload/recategorise/extract-references, email-draft) now calls frontend/web/src/lib/ai/activeProvider.ts getActiveAiCredentials() instead of decrypting a per-user key. Admin manages it at /dashboard/admin/ai-settings (component PlatformAiSettings.tsx, API route /api/admin/ai-settings, role gate founder|admin). Default models centralised in frontend/web/src/lib/ai/models.ts (openai default gpt-5.1, anthropic claude-sonnet-4-6, deepseek deepseek-chat) and mirrored in backend/api/app/services/ai/client.py _DEFAULT_MODELS \u2014 keep both in sync by hand, no shared codegen. AI cost is now the platform's; plan caps/pricing must reflect this (ties into D15/D18 billing enforcement, still web-layer-only)."
    }
  ],
  "operational_notes": [
    {
      "id": "OPS-20",
      "title": "TWO tailored-CV prompts: composition.py is PRODUCTION, tailored_cv.py is LEGACY fallback",
      "detail": "The orchestrator (app/services/pipeline/orchestrator.py ~line 346) branches on get_settings().TAILORED_CV_WRITER == 'w8_verified'. Production sets the w8_verified Fly secret, so the LIVE path is run_tailored_cv_w8_verified \u2192 app/services/eval/writers/_impl.py _writer_w8_integrated \u2192 system prompt built by build_composition_system(role_family, seniority) from app/services/ai/prompts/variants/composition.py (the CAREER-STYLE SUMMARY block lives at composition.py ~lines 278-396). The OTHER prompt, app/services/ai/prompts/TAILORED_CV_SYSTEM in prompts/tailored_cv.py, is the LEGACY writer (run_tailored_cv in pipeline/steps/tailored_cv.py) used ONLY when the w8 flag is off. When changing tailored-CV / summary GENERATION behaviour that must show up in the UI, edit composition.py \u2014 editing tailored_cv.py alone has NO production effect. composition.py is the most behaviour-sensitive file in the repo (tuned by the real-test loop + golden-JD harness); change it deliberately and re-run the golden tests."
    },
    {
      "id": "OPS-21",
      "title": "Career Highlights word-count: 3-way ceiling contradiction fixed (35-50 single source of truth)",
      "detail": "User reported summaries shipping under 25 words despite the composer prompt's '35-50 words HARD MINIMUM' rule. Root cause: the deterministic pipeline only had a MAX-word trimmer (_enforce_career_highlights_words in pipeline/steps/tailored_cv.py, default max_words=65) and an advisory-only validator gate (_gate_profile_word_count in pipeline/steps/tailored_structural_validation.py, pass-band 35-60/absolute-max 80) that the orchestrator logs but never acts on \u2014 nothing ever raised a too-SHORT summary back up. Fix has two parts: (1) added app/services/eval/writers/_impl.py:_ensure_career_highlights_floor(), a one-shot corrective retry wired into _writer_w8_integrated right after to_canonical() and before _enforce_structure() \u2014 if Career Highlights is below 35 words it asks the model to expand with CV-grounded facts only, accepting the retry only if it strictly improves on the original word count (falls back to original on any failure/non-improvement, never loops). (2) aligned all three previously-inconsistent numeric thresholds (50 prompt / 65 trimmer / 60-80 validator) to the same 35-50 target band: trimmer max_words default 65\u219250 (tailored_cv.py), validator pass-band 35-60\u219235-50 with absolute hard-fail moved 80\u219265 (tailored_structural_validation.py). Test tests/test_tailored_cv_step.py::test_enforce_career_highlights_default_cap_is_65 renamed/updated to assert 50. Also merged the years/seniority rule that was stated independently at composition.py ~285-288 (S1 framing), ~368-370 (seniority-adjective ban), and ~392-396 (SENIORITY WORDS) into one authoritative SENIORITY WORDS block; the other two locations now just point to it. Full suite: 960 passed after all edits."
    },
    {
      "id": "OPS-22",
      "title": "Profile availability is an opt-in italic line under Registration & Licences, not a CV section",
      "detail": "Profile credentials JSON gained two fields (contact_details.credentials.availability: subset of [\"Full Time\",\"Part Time\",\"Casual\"]; show_availability: boolean). When show_availability is true AND >=1 type is ticked, build_availability_line (backend/api/app/services/cv/contact_line.py) yields 'Available: ...' text (canonical order via _AVAILABILITY_ORDER) which stamp_credentials renders as its OWN italic markdown line (*Available: ...*) under the ## Registration & Licences heading, separated from the licences line by a blank line. Deliberately NOT a new CV section, so the tailored-CV writer/composition.py is untouched (zero regression risk); availability is also excluded from build_credentials_line (the licences line stays clean). Off by default. Role-gated to nursing+manual. PDF italic via pdf_generator.py _md_inline *...*\u2192<i>. UI: Availability control lives in the ACTIVE unified My CV component frontend/web/src/components/cv/ProfileDetailsClient.tsx CredentialsSection (shown when nursing||manual). NOTE: an earlier copy was added to ProfileSettingsClient.tsx, but the unified My CV page (commit dd0e74b) renders ProfileDetailsClient and only imports TYPES from ProfileSettingsClient \u2014 so the card had to be ported there (this was a real 'I don't see availability' gap). Sanitised/whitelisted in frontend/web/src/app/api/user/preferences/route.ts. JSON-only \u2014 no migration. Tests in test_credentials_stamp.py. Initial chip version shipped via PR #7 (squash-merged to main 2026-06-21); moved to its own italic line in a follow-up on main."
    },
    {
      "id": "OPS-23",
      "title": "MULTI-ROLE summary anchor gap: deterministic net can't repair cherry-picked S2 \u2014 fixed with a generation-time retry",
      "detail": "Bug: when a CV has 2+ multi-month (nameable-anchor) employers, composition.py requires summary S2 to name BOTH (two semicolon clauses). The model sometimes names only one (e.g. the employer that gave an award) and drops the other. The deterministic net _enforce_company_anchor (pipeline/steps/tailored_cv.py:695) could NOT catch this: its 'already anchored' guard uses any(emp in prose for top2) \u2014 so a single named employer short-circuits it \u2014 and its regex append cannot restructure an award-shaped/partially-anchored S2 into two clauses (would double-name). Fix (per D21, honesty enforced at GENERATION time): added _ensure_summary_anchors_both_employers in app/services/eval/writers/_impl.py \u2014 a one-shot corrective retry wired into _writer_w8_integrated right AFTER _ensure_career_highlights_floor and BEFORE _enforce_structure. It reuses _extract_employers_from_cv; when 2+ anchors exist but <2 are named in the summary, it re-asks the model to rewrite S2 into two method+outcome clauses naming both (mixed tense, no tool names, no award-as-S2). Accepts the rewrite ONLY if it now names both top-2 employers AND stays exactly 2 sentences; otherwise keeps the original. Never loops, never fabricates. The deterministic _enforce_company_anchor still runs afterwards as a final no-op safety net. Tests: tests/test_summary_anchor_retry.py (5). Full suite 1160 green. NOTE: _ensure_summary_anchors_both_employers does NOT change the title-slot rule \u2014 opener must still equal the JD job title exactly (separate enforcer _enforce_summary_opener)."
    },
    {
      "id": "OPS-24",
      "title": "Credential prose-capture: trim now stops at orphan ')'/sentence period, not just '('",
      "detail": "Bug: JD credential extraction surfaced 'Cert III in Disability or Individual Support). Strong' \u2014 trailing prose leaked into the credential. Root cause in app/services/skills/post_process.py _trim_qual_phrase: the tail walk only broke on a token STARTING with '(' (the '(or equivalent)' case) plus a small prose stop-word whitelist. Malformed JD text with an ORPHAN closing paren (')' with no matching '(') and prose like 'Strong \u2026' had nothing to break on, so it leaked. Fix: added _CRED_TAIL_TERMINATOR_RE = [).;:] \u2014 when a tail token contains a closing paren / sentence period / ';' / ':', keep the clean prefix before it, drop the rest, stop. Comma is deliberately NOT a terminator (real names like 'ageing, home and community'). Existing '(or' break + prose stop-words unchanged. Tests in tests/test_skills_post_process.py (test_trim_qual_phrase_*). Full suite 1162 green. NOTE: production cv-backend on Fly (jobtrackr-cv-api) must be redeployed (flyctl deploy) for the fix to affect live JD analyses; the rest of the displayed match output (60/60% Care/Soft, WWCC+NDIS missing, Cert IV\u2292Cert III via AQF subsumption) was correct."
    },
    {
      "id": "OPS-25",
      "title": "Teaser-rescue keyword filter was silently disabled (url_hash empty at stage 4b)",
      "detail": "Bug in backend/worker/src/pipeline/keywordFilter.ts applyKeywordFilter: the teaser-rescue path (active when profile.must_include_phrases is set) built its 'already passed by title' set from job.url_hash. But url_hash is only assigned later in the dedup stage (normalise() sets it to ''), so at stage 4b every job's url_hash is ''. Result: when ANY title matched, passedHashes={''} \u2192 titleRejected=jobs.filter(j=>!has(''))=[] \u2192 teaser rescue never ran, so jobs whose TITLE lacks the phrase but whose DESCRIPTION contains it (the whole point of smart-filter rescue) were dropped. Fix: key the passed-set on job.url (canonical URL, already populated by normalise at this stage) instead of url_hash. Verified via tsx repro: a 4-job set with 1 title-match + 2 desc-only matches went from 1 kept (buggy) to 3 kept (fixed). NOTE: only affects profiles using must_include_phrases. Diagnosing a specific 'no new jobs saved' report: each run already logs per-stage kept/dropped counts to run_logs.log_lines (visible in the LiveLogConsole) \u2014 'stage 4b keyword filter: X kept, Y dropped' and 'stage 4c smart filter: ... title missing required / title excluded / desc excluded' pinpoint which gate (and which user rule) zeroes the run. Worker must be redeployed to Fly (flyctl deploy --config backend/worker/fly.toml) for the fix to take effect. CONFIRMED LIVE on profile 2bab2daa (AIN Sydney) via run_logs.log_lines: a 76-job run went stage 4b 76->13 (rescue bug dropped 63 genuine AIN jobs whose title wasn't the exact must_include phrase), then stage 4c 13->2 and stage 7b 2->0 via the user's 'Description must NOT contain' list (home care / Acute Care boilerplate). Rescue fix is the PRIMARY recovery (brings back the 63); desc-exclusion is a secondary trim the user can relax. ALSO ADDED (same commit set): per-phrase attribution on desc-exclusion (excludeByDescription now returns byPhrase; formatExcludeBreakdown renders '[home care: 8, acute care: 3]') logged at stage 4c and 7b, plus 100%-drop tripwire WARNs at stages 4b/4c/7b so a future all-dropped run names the offending rule in the log instantly."
    },
    {
      "id": "OPS-26",
      "title": "Aurora \u2014 signature dual theme (dark hero + light twin) + density control",
      "detail": "New, original design language added as TWO themes: 'aurora-dark' (default) + 'aurora-light'. 100% additive + scoped \u2014 existing 5 themes byte-for-byte unchanged, pipeline/analysis/workflow untouched (CSS + theme registry + fonts only). Signature = teal (--brand) + violet (--violet) 'aurora' gradient, Space Grotesk display headings, Plus Jakarta Sans body, JetBrains Mono numerals, layered elevation, ambient canvas glow. All token pairs WCAG-AA verified (ratios documented in globals.css). Files: (1) globals.css \u2014 two :root.theme-aurora-* token blocks + premium component polish scoped to .theme-aurora-* (buttons=aurora-grad primary, fields, .glass/.kpi-card elevation + gradient accent line + mono value, table hover, left-bar sidebar+glow, selection, ambient body glow) + density (--ui-scale on html/body). (2) lib/themes.ts \u2014 registered both (Theme type, THEMES with previews, VALID_IDS, THEMED_CLASSES); getStoredTheme default flipped 'notion'->'aurora-dark'. (3) layout.tsx \u2014 next/font Plus_Jakarta_Sans/Space_Grotesk/JetBrains_Mono (--font-jakarta/-grotesk/-jbmono; jakarta+grotesk preload, mono lazy; manrope/noto flipped to preload:false as no longer default); FOUC default 'notion'->'aurora-dark' + regex incl aurora-* + density attr. (4) NEW lib/density.ts + components/DensityPickerClient.tsx ('Text size' Compact/Comfortable/Spacious, localStorage 'jobtrackr-density', FOUC-applied). (5) settings/theme/page.tsx \u2014 added density picker. Default density 'comfortable' = --ui-scale 1 = no change for other themes. Public pages (landing/auth) unaffected \u2014 full-bleed with own palettes. Charts (PipelineDonut canvas, PipelineFunnel) auto-adapt via tokens; their fixed slice hexes already read on dark. Verified: tsc clean + next build exit 0 (fonts resolve, Tailwind compiles). Standalone aurora-preview.html exists at repo root (delivered to user, NOT committed). Known minor: a few callout chips use hardcoded Tailwind light palettes (bg-amber-50 etc.) \u2014 readable on dark but not token-themed; future token sweep optional."
    },
    {
      "id": "OPS-33",
      "title": "Credential trim \u2014 drop trailing prose adjectives + dangling connectors ('Bachelor of Nursing and ready')",
      "detail": "Recurring credential prose-capture (follows OPS-24). JD credential extraction surfaced 'Bachelor of Nursing and ready' (and other run-to-run tails) instead of 'Bachelor of Nursing'. _trim_qual_phrase (skills/post_process.py) walked the tail after _QUAL_PATTERN and only stopped on a small prose set + paren/period terminators \u2014 'and'/'ready' passed through. Fix: (1) expanded _CRED_PROSE_TAIL_STOP with prose adjectives/verbs (ready, able, available, eager, keen, enthusiastic, willing, interested, ideally, preferred, desirable, essential, plus, advantageous, looking, seeking, wanting, equivalent, must, should); (2) added _CRED_TAIL_CONNECTORS and a post-walk loop that strips trailing connector/filler tokens (and/or/with/for/to/in/of/a/an/the/&/...) so we never leave a dangling 'Nursing and'. Legit '...and Community' continuations are preserved (the stop fires on the prose word, not on 'and' itself; the connector-strip only trims the LAST token). Verified: 'bachelor of nursing and ready[ to start]' \u2192 'bachelor of nursing'; 'certificate iv in ageing, home and community care' unchanged; OPS-24 orphan-paren case unchanged. Test test_trim_qual_phrase_drops_trailing_prose_adjectives_and_connectors. Full suite 1167. Deploy: cv-backend (flyctl deploy -a jobtrackr-cv-api)."
    },
    {
      "id": "OPS-32",
      "title": "Employer anchor stamped-then-stripped by verify_claims \u2014 re-run anchor net post-verify",
      "detail": "Same stamp-then-strip class as OPS-31 (availability), now for the SUMMARY EMPLOYER ANCHOR. Symptom (Rashmi, post-deploy): S2 = 'Delivers safe, high-quality care, including electronic medication support for residents living with dementia.' \u2014 ZERO employers named despite the CV having Uniting + The Jesmond Group. Root cause: _enforce_company_anchor runs mid-pipeline inside _enforce_structure (_impl.py:654, pre-verify); verify_claims (line 1064) then rewrites the summary and drops the employer name(s), and the anchor net was NEVER re-applied afterward. Fix: re-run _enforce_company_anchor(verified_md, cv_text) in the post-verify chain (imported into _impl.py; placed just before the availability re-stamp). It is idempotent \u2014 no-op when both top-2 employers are already named, ZERO-named \u2192 append 'at E1 and E2.', PARTIAL \u2192 splice missing employer (OPS-30 logic). _find_summary_block matches 'Professional Summary' (the post-verify heading) so the re-run resolves the section correctly. Full suite 1166 passes. Deploy: flyctl deploy --config backend/api/fly.toml. NOTE the user's pasted example was from a deploy predating OPS-30/31/32 \u2014 this commit is the systemic fix that makes the anchor survive verify."
    },
    {
      "id": "OPS-31",
      "title": "Availability line was stamped then stripped by verify_claims \u2014 re-stamp + verify-skip + idempotency",
      "detail": "Root cause of 'availability not appearing on tailored CV' (user-diagnosed, confirmed in code): stamp_availability_in_summary runs mid-pipeline inside _writer_w8_integrated (_impl.py:747), but the w8_verified entry point then calls verify_claims (_impl.py:1064) + a long post-verify re-pass chain that NEVER re-stamps availability. verify._collect_summary bundled the italic '*Available: \u2026*' line into the summary prose (its bullet guard skips '* ' but the note starts '*A' with no space), so when the summary was repaired the line was deleted before storage \u2014 affecting BOTH the .md preview and the PDF (same stored file). THREE fixes: (1) verify.py _collect_summary now skips lines matching ^\\*?\\s*available: (regex) so the verifier never treats availability as a fact-claim. (2) _impl.py run_tailored_cv_w8_verified re-stamps availability LAST (after verify + all re-passes, before result.tailored_md=verified_md). (3) stamp_availability_in_summary made IDEMPOTENT \u2014 strips any pre-existing availability line (and the blank we inserted before it) before re-appending, so the double call never duplicates and produces stable output. Tests: test_availability_stamp_is_idempotent + existing summary tests. Full suite 1166 passes. Deploy: flyctl deploy --config backend/api/fly.toml. (UI copy fix 'Professional Summary' already shipped in OPS-30.)"
    },
    {
      "id": "OPS-30",
      "title": "Anchor enforcer \u2014 partial cherry-pick: splice missing employer + past-tense fix",
      "detail": "Symptom from a v370-deployed run (Rashmi): summary S2 = 'Provide person-centred care\u2026 at Uniting, and have served as a primary Medication Assistant.' \u2014 one employer (Uniting) named, second (The Jesmond Group) dropped. _enforce_company_anchor's original any()-guard treated this as 'already anchored' and skipped. Fix: enforcer now handles THREE cases \u2014 (a) BOTH named \u2192 no-op; (b) ZERO named \u2192 append 'at E1 and E2.' (legacy); (c) PARTIAL \u2192 splice the missing employer into a trailing ', and (have) verb-ed' tail (= the cherry-picked second-role clause) AND convert 'have served' (present-perfect) \u2192 'served' (simple past) per the completed-role tense rule. When no such tail exists, append a fresh '; at <missing>.' clause. Module-level _DANGLING_END_RE moved out of the function. New regression tests in test_tailored_cv_step.py: test_enforce_company_anchor_partial_cherry_pick_splices_missing_employer (Rashmi's exact case) + test_enforce_company_anchor_partial_appends_when_no_tail. Full suite 1165 passes. ALSO updated frontend Availability hint text in ProfileDetailsClient.tsx (stale 'under Registration & Licences' \u2192 'at the end of your Professional Summary') to match OPS-26 stamping behaviour. tsc clean, next build exit 0. Deploy needed: flyctl deploy --config backend/api/fly.toml."
    },
    {
      "id": "OPS-29",
      "title": "Aurora readability round 3 + availability moved into Professional Summary",
      "detail": "UI: (1) Skills-by-category chips (AnalysisRunClient) + ATS count badge were hardcoded bg-[#DDF4FF] \u2192 now bg-[var(--brand)]/10 text-[var(--brand)] border-[var(--brand)]/25 (readable on dark + light, matches the matched/missing keyword chip style). (2) FeasibilityCard TONE_CHIP/TONE_REWRITE + inline violet/amber hexes (#DDF4FF/#FBEFFF/#FFF8C5/#8250DF/#9A6700) \u2192 token-based (var(--brand|purple|amber)/12 chips, /8-tint + text-text rewrite boxes) so the 'Suggested rewrite' / inject boxes are readable on dark. (3) Native <select> got color-scheme:dark (aurora-dark) / light so the OS option popup stops showing the bright-blue browser-default highlight. (4) gh-btn-primary reverted from aurora-grad to SOLID var(--brand) so Apply now / Add job / Try-again match the solid-teal Analyze + Save buttons. (5) Voice 'Recommended \u2014 writing fresh\u2026' guidance text forced to text-text/text-2 (white) per request. BACKEND (CV render): availability MOVED from the ## Registration & Licences block into the Professional Summary \u2014 new contact_line.stamp_availability_in_summary() appends '*Available: \u2026*' (italic) right after the summary paragraph, above the next section; stamp_credentials no longer emits it; wired in _impl.py after stamp_credentials. Tests updated (test_credentials_stamp.py). Verified tsc 0, next build 0, 1163 backend tests pass. NOTE source badges (seek/adzuna=badge-blue, careerjet=badge-teal) already adapt to teal under aurora tokens; screenshots showing blue were pre-deploy."
    },
    {
      "id": "OPS-28",
      "title": "Aurora UI polish \u2014 revert gradient buttons/donut, fix native selects + hardcoded light-mode tints app-wide",
      "detail": "Round-two consistency pass per user review. (1) REVERTED the broad button[bg-[var(--brand)]]\u2192aurora-grad selector \u2014 primary buttons now stay SOLID teal (per user 'lets keep the button green as previously'). Aurora gradient is reserved for KPI card accent + visual identity only. (2) REVERTED donut gradient-ring (removed --donut-grad-* tokens, removed ringGrad/gradStops code in PipelineDonut.tsx); back to per-slice solid colours but in the Aurora palette so legend dots MATCH the donut: Sourcing = teal/violet/cyan (was teal/slate/orange), other lenses use the same family. ATS keeps amber/red for above/below-threshold meaning. (3) Defined --input-border + --input-bg in base :root (were referenced in VoiceCaptureClient textarea but undefined \u2192 invisible box). Also voice textarea text-black \u2192 text-text so caret/typed text is visible on dark. (4) Custom-styled native <select>: appearance:none, surface-2 background, brand-tinted SVG chevron, options use surface bg + brand-tinted :checked highlight (fixes the bright-blue 'Yes' default browser highlight in AHPRA/work-rights dropdowns). (5) Hardcoded LIGHT-MODE Tailwind tints used app-wide (emerald-50/green-50/amber-50/red-50/blue-50/pink-50 + 200-border + 600/700/800 text) re-mapped under .theme-aurora-* via color-mix(brand/green/amber/red/violet, surface) so EVERY callout/guidance/connected box is readable on dark \u2014 fixes Voice 'Write a sample' green box, Voice 'Paste a cover letter' tab guidance, Email 'Gmail connected' (text was white-on-green \u2192 now contrast), ProfileForm FunnelChip (#DDF4FF light blue \u2192 brand-tinted), Voice AI-phrase amber pills, dashboard thin-JD/passed/ready callout chips. (6) ProgressDots icons next to Analyze button (text-blue-600/purple-600/amber-600/green-600) remapped to brand/violet/amber/green tokens so they're visible on the deep canvas. (7) SelectAll 'Select all/Deselect all' button gets a .data-select-all hook + scoped rule = brand-filled chip matching the Select-mode toggle next to it. tsc clean + next build exit 0."
    },
    {
      "id": "OPS-27",
      "title": "Aurora consistency pass \u2014 borders, gradient buttons, checkboxes, chart colours",
      "detail": "Follow-up to OPS-26 fixing review gaps where inline-Tailwind/native markup ignored the theme-scoped class CSS. All fixes are token/selector-level in globals.css (scoped to .theme-aurora-*) + PipelineDonut tokenisation. (1) BORDERS: --card-border/--card-bg were referenced (VoiceCaptureClient, StoriesClient, voice/page) but NEVER defined anywhere \u2192 border-color fell back to currentColor (a bright border on dark). Defined them in base :root = var(--border)/var(--surface) (fixes all themes). Also tinted aurora --border/--border-muted to a consistent brand-tinted hairline via color-mix so every box+input agrees. (2) BUTTONS: primary brand buttons use inline bg-[var(--brand)] not the .gh-btn-primary class, so added a scoped attribute-selector rule giving button/a[class*='bg-[var(--brand)]']:not(rounded-full) the --aurora-grad gradient + hover lift app-wide; rounded-full pills excluded (keep solid teal). (3) CHECKBOXES: native, unthemed \u2192 custom appearance:none gradient box + SVG check (check colour = brand-fg per mode) applied to ALL input[type=checkbox]; radios get accent-color:var(--brand). (4) CHARTS: PipelineDonut slice colours were hardcoded hex \u2192 moved to CSS vars (--chart-pos/info/applied/warn/amber/danger/neutral), defaults preserve old palette, aurora overrides to teal/violet/cyan; added resolveColor() so canvas fillStyle resolves var()\u2192hex (DOM legend dots use var() directly). PipelineFunnel already token-driven (no change). Verified tsc 0 + next build 0."
    },
    {
      "id": "OPS-1",
      "title": "Migration tracking is CLI-untracked",
      "detail": "All migrations applied manually via Supabase SQL editor. If supabase link is ever set up, run supabase migration repair to mark all migrations as applied before using CLI workflows."
    },
    {
      "id": "OPS-2",
      "title": "bullmq + ioredis in frontend/web/package.json are legitimate",
      "detail": "Used by frontend/web/src/app/api/profiles/[id]/run/route.ts and frontend/web/src/lib/actions.ts for BullMQ queue trigger. Server-side only, do not remove."
    },
    {
      "id": "OPS-12",
      "title": "Scrape pipeline observability: run_logs.current_stage + log_lines",
      "detail": "current_stage written by setStage() at each transition, cleared on finishRunLog. log_lines (jsonb) streams console output via append_run_log_line() Postgres RPC. UI: LiveRunStatus banner + LiveLogConsole (collapsible). RunNotifier toast in dashboard layout. Polling: LiveRunStatus 3s/12s, LiveLogConsole 4s+1.5s, RunNotifier 3s."
    },
    {
      "id": "OPS-13",
      "title": "curl_cffi bypasses Cloudflare from Fly datacenter without proxy",
      "detail": "Python curl_cffi impersonating Chrome 124 TLS bypasses Cloudflare from Fly.io IPs. SEEK + Careerjet return HTTP 200. TypeScript spawns python3 scripts/fetch_jd.py as subprocess; JSON on stdout. Subprocess overhead ~200ms \u2014 negligible vs HTTP round-trip."
    },
    {
      "id": "OPS-14",
      "title": "Careerjet API replaced with direct HTML scraping due to jobviewtrack.com blocks",
      "detail": "Careerjet's API uses jobviewtrack.com for click tracking, which strictly blocks Fly.io Datacenter IPs with 404s. To avoid relying on an Apify residential proxy, the adapter was rewritten (2026-05-27) to scrape careerjet.com.au HTML directly using curl_cffi. WARNING: HTML scraping is fragile. If the DOM changes or Careerjet strictly rate-limits Fly's IP under high load, the documented fallback plan is to revert to the API adapter and use an Apify Residential proxy."
    },
    {
      "id": "OPS-15",
      "title": "Auth is password-only; master admin account",
      "detail": "Magic-link (signInWithOtp) removed from /auth/login 2026-05-25 \u2014 email+password only. Master admin: admin@jobtrackr.com (role 'founder' in public.users). Password NOT stored here; set/rotate via Supabase Dashboard \u2192 Authentication \u2192 Users (Add user, Auto Confirm). New auth users get a public.users row automatically via the on_auth_user_created trigger (default role 'beta'); promote with: update public.users set role='founder' where id=(select id from auth.users where email='admin@jobtrackr.com'). Admin route pages (/dashboard/admin, /dashboard/admin/metrics) still exist but are unlinked from the sidebar."
    },
    {
      "id": "OPS-16",
      "title": "Never import runtime values from a 'use client' module into a server component",
      "detail": "Next.js returns a client-reference proxy (not the real value), so arithmetic \u2192 NaN and the render crashes. This caused the instructions ?step=N 'This page couldn't load' bug. Fix pattern: keep shared constants/data in a neutral plain module (e.g. frontend/web/src/lib/setupSteps.ts) that both server and client import."
    },
    {
      "id": "OPS-17",
      "title": "Dashboard empty state has two modes (gate = !hasAnyJob)",
      "detail": "frontend/web/src/app/(dashboard)/dashboard/page.tsx: when no jobs exist, coreComplete = status.profile && status.cv && status.aiKey. !coreComplete \u2192 FirstRunScreen (Get-set-up wizard / SetupCards). coreComplete \u2192 ReadyToScanScreen (create/run a search-profile CTA + the How-it-works deck). The How-it-works deck is the shared component frontend/web/src/components/onboarding/HowItWorksDeck.tsx (built on SwipeDeck.tsx) reused by both this screen and the instructions 'How it works' tab."
    },
    {
      "id": "OPS-18",
      "title": "Profile Location field uses Google Places via a server-side proxy",
      "detail": "frontend/web/src/app/api/places/autocomplete/route.ts (auth-gated GET ?q=) calls the Places Autocomplete API (New) at places.googleapis.com/v1/places:autocomplete, restricted includedRegionCodes:['au'] + includedPrimaryTypes:['(cities)']. Key is PLACES_API_KEY (server-only, NOT NEXT_PUBLIC_) so it never reaches the browser. UI: frontend/web/src/components/LocationAutocomplete.tsx \u2014 controlled input + 300ms-debounced dropdown, still free-typeable (Remote etc.), strips trailing ', Australia' on select to keep the 'Sydney NSW' shape the worker sources expect; replaces the plain input in ProfileForm.tsx. Requires 'Places API (New)' enabled in GCP (distinct from legacy Places API) + billing. Do NOT add an HTTP-referrer key restriction \u2014 calls are server\u2192Google from Vercel IPs; an API restriction (limit to Places API New) is fine."
    },
    {
      "id": "OPS-19",
      "title": "Platform-wide AI provider replaces BYOK \u2014 migration 060 not yet applied",
      "detail": "Migration shared/supabase/migrations/060_platform_ai_settings.sql creates public.platform_ai_settings, best-effort copies any existing founder/admin BYOK key from user_integrations, picks one active row, then DELETEs all anthropic/openai/deepseek rows from user_integrations (irreversible from the app). RISK flagged by migration-checker: if no founder/admin had a previously-valid key, the seeded 'openai' row is left with status=NULL/no key, so NO user's analyses work until an admin opens /dashboard/admin/ai-settings and pastes+validates a key. Required steps before applying: (1) read-only check that a founder/admin user_integrations row has status='valid' and a key for at least one provider; (2) optionally snapshot user_integrations before running, since the DELETE has no soft-delete/audit trail; (3) after applying, verify `select * from platform_ai_settings` shows exactly one is_active=true row with status='valid', and `select count(*) from user_integrations where provider in ('anthropic','openai','deepseek')` returns 0; (4) if the active row isn't valid, immediately set one via the admin UI to close the AI-dark window. See D20 for the full file list."
    }
  ],
  "known_issues": [
    {
      "id": "BUG-1",
      "title": "DOCX upload fails with ERR_TIMED_OUT to Supabase Storage",
      "severity": "low",
      "workaround": "Convert DOCX to PDF before uploading. PDF path fully functional."
    },
    {
      "id": "BUG-2",
      "title": "GPT-5* family forced to temperature=1 \u2192 noisier output",
      "severity": "medium",
      "detail": "OpenAI enforces temperature=1 on gpt-5* (HTTP 400 otherwise). We strip temperature so call succeeds; user trades determinism for capability. Prefer Anthropic Sonnet 4.6."
    },
    {
      "id": "BUG-5",
      "title": "Jora adapter hangs Chromium on 512MB Fly worker \u2014 disabled",
      "severity": "medium",
      "detail": "Commented out in backend/worker/src/sources/index.ts. Fix: (A) fly scale memory 1024; (B) rewrite to HTTP/SSR parse; (C) drop Jora."
    }
  ],
  "deferred_features": [
    {
      "id": "DEFERRED-0",
      "title": "COMPLETED 2026-06-16: Deleted dead eval harnesses (Scope A + B)",
      "detail": "Writer-variant eval (Scope A, commit 99aaafe) + source-eval beta tool (Scope B, commit 57a75fd). Both unused for 3+ weeks despite ongoing work. Removed ~2000 LOC, Redis worker, BullMQ queue, Supabase table writes. Recovery: git history + branches feat/writers-eval-beta and feat/source-eval-beta."
    },
    {
      "id": "DEFERRED-1",
      "title": "Tier 4 user-initiated JD URL fetch",
      "file": "frontend/web/src/lib/scrapeJobUrl.ts",
      "status": "built, not wired \u2014 no API route calls it",
      "to_activate": "Create POST /api/jobs/scrape-url. Wire to 'Fetch from URL' button on JobEditModal."
    },
    {
      "id": "DEFERRED-2",
      "title": "Multi-source shared jobs pool for scale (1000s of users)",
      "status": "designed, not built \u2014 current per-user live fetch is fine for 10-20 test users",
      "reconsider": "When approaching 50-100 daily active users or source rate-limit errors appear."
    },
    {
      "id": "DEFERRED-3",
      "title": "Split oversized service files (1k+ lines)",
      "status": "watch list \u2014 refactor opportunity, no behaviour change required",
      "candidates": [
        "backend/api/app/services/eval/writers/_impl.py (1542 lines \u2014 split per writer variant)",
        "backend/api/app/services/pipeline/steps/tailored_structural_validation.py (1265 lines \u2014 split per validator)",
        "backend/api/app/services/ai/prompts/tailored_cv.py (1234 lines \u2014 split by section/variant)",
        "backend/api/app/services/eval/enforce_w3.py (1139 lines \u2014 VERIFY DEAD: production writer is w8_verified; w3 may be deletable entirely)",
        "backend/api/app/services/pipeline/steps/tailored_cv.py (841 lines \u2014 orchestrator + helpers)",
        "backend/api/app/services/eval/role_families.py (706 lines \u2014 extract role-family configs to JSON)"
      ],
      "when_to_act": "When touching one of these files for an unrelated reason, or when a feature needs to add to it. Do NOT do a big-bang split \u2014 high regression risk."
    },
    {
      "id": "GLOBAL-BUCKET",
      "title": "Global job bucket \u2014 shared, deduplicated job pool across users",
      "plan": "docs/global-job-bucket-plan.md",
      "status": "LIVE + hardened. Serve distance-bound (46/47 validated), single-flight locks + skip-when-fresh shipped (c4692ed), admin observability shipped (614fba5). Run-cap: no change (every run counts). Upgrade-on-read DEFERRED until weekly users exist.",
      "detail": "Goal: one global 30-day bucket every user reads first; a fetch scrapes only the delta of days not yet covered for its search slice (keyword x location-cell x source). Filter-on-read so one user's exclusions never starve another (home-care vs aged-care). Tier rule (existing): SEEK full JD shared with all; Adzuna full JD shown only to unlimited even when cached; weekly/monthly see Adzuna snippet. 30-day retention; eviction keeps rows referenced by applications/analysis.\n\nPHASE A done this session (additive, safe, flag-free because inert):\n  - migration 066_search_coverage.sql (NOT yet applied in Supabase \u2014 manual SQL editor step).\n  - backend/worker/src/pipeline/coverage.ts (resolveSlices/normaliseKeyword/recordCoverage write-only; readCoverage/computeDeltaDays defined for Phase B but NOT called).\n  - orchestrator.ts wires recordCoverage() after a successful run, try/catch-guarded (missing table no-ops). Worker typecheck passes. No behavior change to existing scrape depth.\n\nKEY DECOMPOSITION DECISION: acting on coverage (scrape-delta + bucket serve) is COUPLED to the global_jobs/profile_jobs split \u2014 making B's run scrape only the delta without serving it the rows A already has would REGRESS B's results. So lookback-flip + serve ship together in Phase B, not before. Phase A only warms the ledger.\n\nPHASE B (next, needs gates): migration-checker first; global_jobs (bucket, snippet+full JD + jd_access) + profile_jobs (RLS per-user state); backfill jobs; dual-write; reads behind flag; eviction cron. OPEN DECISION #1: keep analysis_runs.job_id on jobs vs repoint to profile_jobs (shapes backfill/cutover) \u2014 settle before B.\nPHASE C: single-flight slice lock (refreshing/refresh_started_at) + Adzuna upgrade-on-read.\nPHASE D: admin observability + cutover. NEW OPEN ITEM: bucket-served runs vs the live weekly=30/monthly=120 run caps (enforced web-layer only today) \u2014 decide whether a 0-scrape bucket read counts against the cap.\n\nVERIFICATION REALITY: worker has no test harness (tsc --noEmit only); validation is the Vercel-preview gate per phase.\n\nPHASE B FOUNDATION done this session (dormant, flag-guarded, pushed to main):\n  - migration 067_global_jobs.sql (canonical bucket: url_hash unique, snippet+full JD + jd_access, matched_keywords gin, lat/lng null until geocode-on-write, service-role RLS). NOT applied.\n  - migration 068_profile_jobs.sql (per-user link+state, RLS via search_profiles mirroring jobs). NOT applied.\n  - backend/worker/src/pipeline/bucket.ts: dualWriteBucket() behind USE_GLOBAL_BUCKET env flag (default off). Best-effort try/catch (missing table no-ops). Merges matched_keywords across runs. deriveJdAccess: seek/careerjet/board='all', adzuna direct='unlimited_only', adzuna api='snippet'.\n  - orchestrator.ts calls dualWriteBucket after saveJobs. jobs stays source of truth; nothing reads bucket yet.\n  - Worker typecheck passes. With flag OFF, save path is byte-for-byte unchanged -> zero production impact.\n\nSTILL DORMANT/NOT DONE (need preview gate + decisions, NOT pushed):\n  - Backfill migration (069) collapsing existing jobs into global_jobs/profile_jobs (one-time, over prod data).\n  - Web read-path switch (dashboard/applications/analyze reading profile_jobs behind a flag) \u2014 the production-affecting part; deliberately NOT done blind.\n  - Coverage-driven lookback flip (couples with serve; ship with read switch).\n  - Phase C single-flight lock + Adzuna upgrade-on-read. Phase D observability + analysis_runs FK repoint (decision #1) + cutover.\n  - Precise snippet-vs-full JD split (foundation approximates description as both; refine at read build).\n\nTO ACTIVATE B: apply 067+068 in Supabase SQL editor, set worker env USE_GLOBAL_BUCKET=true (fly secrets), run a profile, verify global_jobs/profile_jobs populate. Still no UI change until read path is built.\n\n=== ARCHITECTURE PIVOT 2026-06-25: serve-into-jobs ===\n31 web sites read jobs + analysis_runs.job_id FKs jobs.id -> rejected switching reads to profile_jobs (too large/risky). CHOSEN: worker serves the bucket INTO jobs. global_jobs(067)+search_coverage(066) are the shared bucket+ledger; worker scrapes coverage-driven delta, upserts to global_jobs, then materialises each profile's jobs rows FROM the bucket (tier-projected JD + profile filters + per-user distance). RESULT: zero web changes, NO analysis_runs FK repoint (decision #1 MOOT), profile_jobs(068) reserved/unused. All gated by USE_GLOBAL_BUCKET (default off).\n\nBUILT on feat/global-bucket (typecheck clean, NOT merged, NOT deployed, migrations NOT applied):\n  - bucket.ts: bucketEnabled flag; upsertGlobalJobs (canonical upsert + matched_keywords merge + deriveJdAccess seek/careerjet/board=all, adzuna-direct=unlimited_only, adzuna-api=snippet); serveProfileFromBucket (select by location_cell+30d window -> map -> tier JD projection (adzuna full gated to unlimited) -> applyKeywordFilter+postFetchFilter+working-rights -> per-user distanceFor -> NormalisedJob[] for saveJobs); evictStaleBucket (DELETE first_seen_at<30d, safe because global_jobs referenced by nothing).\n  - coverage.ts: sliceDeltaDays (cold=30d, fresh<6h=0, else days+1), computeProfileLookback (max delta across slices, allFresh), acquireSliceLocks/releaseSliceLocks (atomic single-flight, BUILT but NOT wired -- herd protection currently via TTL-driven lookback=0).\n  - orchestrator.ts: bucket-mode lookback (coverage-driven) replaces per-profile run_logs lookback; after stage 11b -> upsertGlobalJobs(scraped) then serveProfileFromBucket replaces toSave; evictStaleBucket at run start. Flag off = unchanged.\n  - scripts/backfillBucket.ts: one-off warm-start seeding global_jobs from existing jobs (reuses upsertGlobalJobs for correct location_cell). Run: USE_GLOBAL_BUCKET=true tsx backfillBucket.\n\nSTILL OPEN / NOT DONE:\n  - Phase C single-flight NOT wired (TTL covers most herd cases); Adzuna upgrade-on-read for unlimited readers of snippet-only cached rows = TODO (they see snippet until next scrape).\n  - Phase D admin observability (bucket hit-rate / scrapes avoided / cost saved); bucket-served-run vs live run caps (weekly=30/monthly=120) policy decision.\n  - matched_keywords used only as merge field; serve selects by location_cell+window (filters do precise gating) -- matched_keywords gin prefilter is an optimization not yet used.\n  - geocode-on-write (store lat/lng on global_jobs) deferred; distance computed per-serve via distanceFor(location string) which is cached.\n\nTO VALIDATE (preview gate): merge or check out branch; apply 066+067 in Supabase; set worker USE_GLOBAL_BUCKET=true; optionally run backfillBucket; run two profiles same keyword+location and confirm 2nd scrapes ~0 days yet sees full window; weekly sees adzuna snippet, unlimited sees full.\n\nGEOCODE-ON-WRITE (2026-06-25, branch): global_jobs.lat/lng geocoded ONCE per posting in upsertGlobalJobs (reuses stored coords on repeat); serveProfileFromBucket computes per-user distance via new distanceFromCoords() (OSRM only, NO Nominatim 1.1s-gap geocode); orchestrator skips the stage-11b distance loop when bucketEnabled. Fixes the first-run distance bottleneck (~100 jobs x Nominatim = 650s) for any posting already bucketed. First scrape of a new posting still pays one-time Nominatim geocode.\n\n=== HARDENING 2026-06-25 (all on main, deployed) ===\nA. SINGLE-FLIGHT + SKIP-WHEN-FRESH (c4692ed): all-fresh slices skip stage 2 entirely (0 scrape); stale slices -> first caller claims per-slice refresh lock + scrapes, others skip+serve; cold slices (no coverage row) ALWAYS scrape (never skipped). Locks released post-run; 10-min stale guard for crashes. Validated earlier: lookback floor fix (0c2b7ba) + serve distance-bound (407cae7) + geocode region-bias (3ec4659) + geocode-on-write (38aa9d2).\nC. OBSERVABILITY (614fba5): admin Sourcing page Global-bucket card (size, slices+fresh, scrapes avoided = completed runs fetched 0 saved >0, hit rate, per-source). Best-effort queries.\nRUN-CAP DECISION: no change. Every pipeline run counts vs weekly-30/monthly-120 regardless of scrape amount; bucket makes runs cheaper not free; AI cost gated separately by CV/letter caps.\nDEFERRED (by design): upgrade-on-read (Adzuna full-JD for unlimited readers of snippet-only rows) \u2014 zero effect until non-unlimited users populate snippet rows; adds Apify cost per serve; untestable now. Wire when first weekly/monthly user onboards. Also still: drop unused profile_jobs table; fold non-date sources; validate a real weekly-tier run shows Adzuna snippet."
    }
  ]
}